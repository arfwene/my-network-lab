# Network Lab 마스터 플랜

> 대상: 네트워크 기초 지식이 거의 없는 신입 네트워크 엔지니어
> 목표: 개념 암기가 아니라 **"직접 만들고, 부수고, 패킷으로 확인"** 하는 경험을 통해 실무 감각 습득

---

## 1. 최종 학습 목표 (Exit Criteria)

### 1.1 숙련도 레벨 정의

| 레벨 | 의미 | 판정 방법 |
|---|---|---|
| **L1 · 설명** | 개념을 자기 말로 설명하고 그림으로 그릴 수 있다 | 구술 / 서면 확인 문제 |
| **L2 · 구현** | 요구사항을 받아 직접 설정하고 정상 동작을 검증할 수 있다 | 실습 과제 + `verify.sh` |
| **L3 · 진단** | 고장난 상태에서 원인을 격리하고 **근거를 제시해** 복구할 수 있다 | 장애 주입 챌린지 |

> **과정 목표**: 전 영역 **L2 이상**, 핵심 영역(주소 설계 · 라우팅 · 패킷 분석 · 트러블슈팅)은 **L3** 도달.

---

### 1.2 역량 도메인별 목표

#### A. 네트워크 동작 모델
| # | 목표 | Lv | 모듈 |
|---|---|---|---|
| A1 | TCP/IP 4계층의 각 계층 책임을 설명하고, `curl http://web` 한 번에서 일어나는 **DNS → ARP → TCP handshake → HTTP** 전 과정을 순서대로 나열할 수 있다 | L1 | **M9** (2.8 · 실습 8) |
| A2 | 캡슐화/역캡슐화를 설명하고, 실제 캡처한 프레임에서 Ethernet / IP / TCP / HTTP 헤더의 경계를 지목할 수 있다 | L1 | M8 |
| A3 | 계층별 주소(MAC / IP / 포트)와 **그 주소가 유효한 범위**(로컬 세그먼트 / 엔드투엔드 / 프로세스)를 구분해 설명할 수 있다 | L1 | M2, M7 |

#### B. L1 / L2 — 물리 · 이더넷 · 스위칭
| # | 목표 | Lv | 모듈 |
|---|---|---|---|
| B1 | 인터페이스 상태(up/down, speed, duplex, error·drop 카운터)를 확인해 **물리 계층 이상 여부를 1차 판정**할 수 있다 *(실무 트러블슈팅의 첫 단계)* | L3 | M1 |
| B2 | 스위치의 MAC 학습 · 유니캐스트 플러딩 · 에이징을 설명하고, MAC 테이블을 읽어 다음 홉 포트를 예측할 수 있다 | L2 | M1 |
| B3 | 브로드캐스트 도메인과 충돌 도메인을 구분하고, VLAN으로 도메인을 분할 · 트렁크로 연장할 수 있다 | L2 | M2 |
| B4 | ARP 동작을 설명하고, ARP 테이블을 **근거로** L2 도달성을 판정할 수 있다 | L2 | M1 |
| B5 | Native VLAN 불일치 · VLAN 미할당 등 전형적 L2 오설정의 **증상을 예측**할 수 있다 | L3 | M2 |
| B6 | L2 루프가 왜 치명적인지(브로드캐스트 스톰, MAC 플래핑) 설명하고 STP의 역할을 설명할 수 있다 | L1 | M1 |

#### C. L3 — IP 주소 체계 및 설계
| # | 목표 | Lv | 모듈 |
|---|---|---|---|
| C1 | 서브넷 마스크 / CIDR을 **도구 없이** 계산해 네트워크 주소 · 브로드캐스트 주소 · 사용 가능 호스트 범위를 도출할 수 있다 | L2 | M2 |
| C2 | 조직 요구사항(사이트 · 부서 · 증설 여지)을 받아 **중복 없고 요약(summarization) 가능한 IP 주소 설계안**을 작성할 수 있다 *(네트워크 엔지니어의 핵심 산출물)* | L3 | M2 |
| C3 | IPv6 주소를 축약/확장하고 주소 유형(Link-local / GUA / ULA / Multicast / Anycast)을 구분할 수 있다 | L2 | M6 |
| C4 | IPv4의 ARP · DHCP · 브로드캐스트에 대응하는 IPv6의 **NDP · SLAAC · 멀티캐스트**를 대조 설명하고 dual-stack을 구성할 수 있다 | L2 | M6 |
| C5 | MTU · 프래그멘테이션 · PMTUD를 설명하고, MTU 불일치의 전형적 증상(**ping은 되는데 큰 전송만 실패**)을 진단할 수 있다 *(실무 단골 장애)* | L3 | M2, M4 |
| C6 | DHCP 동작(DORA)과 릴레이가 필요한 이유를 설명할 수 있다 | L1 | M2 |
| C7 | **사설 IP(RFC1918)와 공인 IP를 구분**하고, 사설 주소가 등장한 배경(IPv4 주소 고갈)과 인터넷 통신 시 NAT가 필요한 이유를 설명할 수 있다 | L1 | M2, M10 |
| C8 | 사설 대역 선정 시의 **실무 리스크**(타 조직·VPN 상대와의 대역 중복, `192.168.0.0/24`·`192.168.1.0/24` 남용)를 설명하고 회피 전략을 제시할 수 있다 | L1 | M2 |
| C9 | IPv6의 GUA / ULA를 공인 / 사설 IP 개념과 **대조**하고, IPv6에 NAT가 기본적으로 필요 없는 이유를 설명할 수 있다 | L1 | M6 |

#### D. 경로 결정 — 스위칭과 라우팅
| # | 목표 | Lv | 모듈 |
|---|---|---|---|
| D1 | **같은 대역 통신과 다른 대역 통신에서 목적지 MAC이 어떻게 달라지는지**(IP는 그대로, MAC만 바뀐다) 설명하고 캡처로 증명할 수 있다 *(L2/L3 차이의 결정적 판별)* | L2 | M2 |
| D2 | 라우팅 테이블을 읽고 **Longest Prefix Match**로 특정 목적지의 다음 홉을 정확히 예측할 수 있다 | L2 | M3 |
| D3 | 정적 라우팅을 **양방향으로** 구성하고, 리턴 경로 누락 시의 증상을 진단할 수 있다 | L3 | M3 |
| D4 | AD(Administrative Distance)와 메트릭의 차이, 정적 vs 동적 라우팅의 선택 기준을 설명할 수 있다 | L1 | M3, M4 |
| D5 | OSPF 인접(Neighbor) 형성 조건을 알고 설정 · 검증할 수 있으며, **인접이 안 맺히는 원인 상위 4가지**(Area / 서브넷 · 마스크 / MTU / 인증 · 타이머)를 순서대로 점검할 수 있다 | L3 | M4 |
| D6 | 링크 장애 시 경로 수렴을 **관측**하고, cost 조정으로 의도한 경로로 트래픽을 유도할 수 있다 | L2 | M4 |
| D7 | AS와 BGP의 역할, 사내망(IGP)과 인터넷 경로(EGP)의 구분을 설명할 수 있다 | L1 | M4 |

#### E. L4 — 세션과 포트
| # | 목표 | Lv | 모듈 |
|---|---|---|---|
| E1 | TCP와 UDP의 차이 및 **선택 기준**을 설명할 수 있다 | L1 | M7 |
| E2 | 소켓 4-tuple과 "세션"의 정의를 설명하고, `ss`로 TCP 상태 전이(SYN_SENT → ESTABLISHED → FIN_WAIT → TIME_WAIT)를 추적할 수 있다 | L2 | M7 |
| E3 | **연결 실패 증상만 보고 원인을 구분**할 수 있다 — 경로 없음 / 방화벽 DROP(타임아웃) / 방화벽 REJECT · 포트 미개방(즉시 거부) / 서비스 다운(연결은 되나 무응답) *(실무 최고빈도 문의)* | L3 | M7, M10 |
| E4 | Well-known / Ephemeral 포트 범위를 알고, NAT · 방화벽이 포트를 어떻게 다루는지 설명할 수 있다 | L1 | M7, M10 |
| E5 | 3-way handshake, 4-way termination, 재전송 · 윈도우의 역할을 설명할 수 있다 | L1 | M7 |

#### F. 응용 서비스 — DNS · HTTP · FTP
| # | 목표 | Lv | 모듈 |
|---|---|---|---|
| F1 | **DNS 해석 실패인지 네트워크 도달 실패인지 구분**할 수 있다 (`dig` 결과 vs IP 직접 접속 비교) *("인터넷이 안 돼요"의 1차 분기점)* | L3 | M9 |
| F2 | `telnet` / `nc` 만으로 HTTP 요청을 **수동 작성해 응답을 받아낼** 수 있고, 주요 상태 코드로 책임 소재(클라이언트 / 서버 / 경유 장비)를 판단할 수 있다 | L2 | M9 |
| F3 | HTTP와 HTTPS의 차이, TLS handshake가 **네트워크 관점에서** 무엇을 추가하는지 설명할 수 있다 | L1 | M9 |
| F4 | FTP의 제어 채널 / 데이터 채널 분리를 설명하고, **Active와 Passive의 연결 방향 차이를 캡처로 증명**할 수 있다 | L2 | M9 |
| F5 | FTP Active 모드가 NAT · 방화벽 환경에서 실패하는 이유를 설명할 수 있다 | L1 | M9, M10 |

#### G. 보안 경계 — NAT · 방화벽
| # | 목표 | Lv | 모듈 |
|---|---|---|---|
| G1 | SNAT / DNAT / PAT를 구분해 구성하고, NAT 테이블로 주소 변환을 **추적**할 수 있다 | L2 | M10 |
| G2 | Stateful 방화벽의 동작(리턴 트래픽 자동 허용)을 설명하고 **정책 방향성**을 올바르게 설계할 수 있다 | L2 | M10 |
| G3 | 정책 순서 · 방향 오류로 인한 통신 실패를 진단할 수 있다 | L3 | M10 |
| G4 | 관리 접근에 telnet 대신 SSH를 써야 하는 이유를 **캡처로** 설명할 수 있다 *(telnet은 진단 도구로만 쓴다)* | L1 | M9 |

#### H. 관측과 분석
| # | 목표 | Lv | 모듈 |
|---|---|---|---|
| H1 | 문제를 좁히기 위해 **어디서 캡처해야 하는지 스스로 결정**할 수 있다 *(tcpdump 실력의 본질은 필터가 아니라 캡처 지점 선정)* | L3 | M8 |
| H2 | BPF 필터(`host` / `net` / `port` / `tcp[tcpflags]` / `vlan` / `icmp` + 논리 조합)를 작성해 필요한 패킷만 추출할 수 있다 | L2 | M8 |
| H3 | **다중 지점 동시 캡처**로 패킷이 소실되는 구간을 특정할 수 있다 | L3 | M8 |
| H4 | pcap을 저장 · 전달하고 Wireshark로 스트림을 재구성해 해석할 수 있다 | L2 | M8 |
| H5 | ping / traceroute의 동작 원리(ICMP, TTL)와 **한계**(ICMP 차단 · 비대칭 경로 환경에서의 오독)를 설명할 수 있다 | L2 | M3 |

#### I. 트러블슈팅 방법론 · 엔지니어링 실무
| # | 목표 | Lv | 모듈 |
|---|---|---|---|
| I1 | 장애 신고를 받으면 **영향 범위를 먼저 규정**할 수 있다 (누가 / 어디서 / 언제부터 / 전부인가 일부인가 / 최근 변경 있었나) | L3 | M11 |
| I2 | Bottom-up · Top-down · Divide-and-Conquer를 상황에 맞게 선택하고, **표준 3-step**(① 링크 살아있나 ② L3 도달하나 ③ 포트 · 서비스 응답하나)으로 절단 지점을 격리할 수 있다 | L3 | M11 |
| I3 | 추측이 아니라 **증거(명령어 출력 · 패킷 캡처)로 원인을 입증**할 수 있다 | L3 | M11 |
| I4 | 변경 전 현재 상태를 백업하고 **롤백 계획을 준비한 뒤** 작업할 수 있다 *(엔지니어의 기본 습관)* | L2 | 전 과정 |
| I5 | 장애 보고서를 작성할 수 있다 (현상 / 영향 범위 / 타임라인 / 원인 / 조치 / 재발 방지) | L2 | M11 |
| I6 | 네트워크 구성을 문서화할 수 있다 (토폴로지도 · IP 관리대장 · 변경 이력) | L2 | 전 과정 |

---

### 1.3 졸업 판정 — 캡스톤 통과 조건

제한 시간 **3시간**, 5~7개 장애가 동시 주입된 랩에서 아래를 모두 충족해야 수료.

- [ ] 전 서비스 정상화 — 사이트 간 IPv4/IPv6 통신, web HTTP 응답, FTP Passive 전송, DNS 정상 해석
- [ ] 각 장애에 대해 **원인 · 근거(캡처 또는 명령어 출력) · 조치**를 보고서로 제출
- [ ] 복구 과정에서 **새로운 장애를 만들지 않을 것** (변경 전 상태 기록 · 롤백 가능)
- [ ] 미해결 항목이 있다면 **"어디까지 확인했고 다음에 무엇을 볼 것인지"** 를 기술 *(실무 인수인계 능력)*

---

### 1.4 이번 과정에서 다루지 않는 것 (Out of Scope)

기초 역량 확보를 우선하기 위해 아래는 **명시적으로 제외**한다. 수료 후 별도 과정으로 다룬다.

- 무선(Wi-Fi), QoS, 멀티캐스트 라우팅, MPLS / SD-WAN
- IPsec · SSL VPN 심화, 로드밸런서 · 프록시 심화
- 네트워크 자동화 심화 (교육생은 자동화의 *사용자*이지 작성자가 아님)
- 벤더 특화 기능 및 고급 설계 (이중화 설계, 데이터센터 패브릭 등)

*단, **STP · DHCP · BGP**는 "왜 필요한가"를 설명할 수 있는 **L1 수준까지만** 다룬다. 실무에서 마주칠 때 개념이 없으면 곤란하기 때문.*

## 2. 설계 원칙

1. **하나의 토폴로지가 자라난다** — 모듈마다 새 랩을 만들지 않는다. PC 2대에서 시작해 최종적으로 멀티사이트 네트워크가 된다. 신입은 "왜 이 장비가 필요한가"를 필요해지는 순간에 배운다.
2. **개념 → 손 → 패킷** — 모든 모듈은 (a) 짧은 개념 설명 → (b) 직접 설정 → (c) tcpdump로 눈으로 확인, 순서를 지킨다. tcpdump는 마지막 모듈이 아니라 **Module 1부터 계속 쓴다.**
3. **일부러 부순다** — 각 모듈 끝에 "고장난 상태"를 주고 고치게 한다. 성공 경로만 따라가면 트러블슈팅을 못 배운다.
4. **코드로 재현 가능** — 토폴로지·설정을 전부 텍스트 파일로 관리. 신입이 랩을 망가뜨려도 1분 안에 초기화 가능해야 실험을 두려워하지 않는다.
5. **벤더 중립 → 벤더 특화** — 먼저 프로토콜 자체를 배우고, 회사 실장비 CLI는 나중에 매핑한다.

---

## 3. 최종 토폴로지 (Module 10 도달 시)

```
   [ Site-A / 지사 ]              [ Core ]              [ Site-B / 데이터센터 ]

  pc1 ─┐                        ┌── r2 ──┐                        ┌─ web  (HTTP/HTTPS)
       ├─ sw1 ─── r1 ───────────┤        ├─── r4 ─── sw2 ─────────┼─ ftp  (FTP)
  pc2 ─┘  (VLAN10/20)           └── r3 ──┘                        └─ dns  (DNS)
                                   ↑
                          이중 경로: OSPF 수렴·경로 우회 실습용

                                   │
                              [ edge/NAT ] ─── inet (외부 인터넷 흉내)
```

**모듈별 토폴로지 성장 단계**

| 모듈 | 추가되는 것 | 배우는 것 |
|------|-------------|-----------|
| M1 | pc1, pc2, sw1 | L2, MAC, ARP, 스위칭 |
| M2 | VLAN 10/20, r1 | 브로드캐스트 도메인, L3 경계, 게이트웨이 |
| M3 | r2, r4, sw2, web | 정적 라우팅, 라우팅 테이블 |
| M4 | r3 (이중 경로) | 동적 라우팅(OSPF), 수렴, 경로 우회 |
| M6 | 전 구간 IPv6 | Dual-stack, NDP, SLAAC |
| M7 | (서버 활용) | TCP/UDP, 포트, 세션 |
| M8 | — | tcpdump 심화 |
| M9 | ftp, dns | HTTP, DNS, FTP |
| M10 | edge/NAT, inet | NAT, 방화벽, FTP Passive의 이유 |
| M11 | 장애 주입 | 트러블슈팅 캡스톤 |

---

## 4. 모듈 로드맵

각 모듈은 **2~4시간** 분량. 총 10모듈 + 오리엔테이션 = 약 30~40시간 (2주 과정 또는 주 1회 10주 과정).

### M0. 오리엔테이션 & 랩 환경 구축 (1h)
- 랩 도구 설치, 토폴로지 기동/정지/초기화 방법
- "장비에 접속한다"는 것의 의미 (콘솔 vs SSH vs 컨테이너 exec)
- 앞으로 쓸 도구 지도: `ip`, `ping`, `traceroute`, `ss`, `tcpdump`, `curl`
- **과제**: 랩을 띄우고 모든 노드에 접속해 hostname 확인, 랩 초기화 1회 수행

### M1. 랜선 한 가닥부터 — L1/L2와 스위칭 (3h)  → B1~B6, A3
- 개념: 물리 계층, 이더넷 프레임, MAC 주소, 브로드캐스트/유니캐스트, 스위치 MAC 학습, 충돌/브로드캐스트 도메인
- **실무 1차 확인 절차**: 인터페이스 up/down, speed/duplex, error·drop 카운터 읽는 법 → "장애 신고 오면 여기부터 본다"
- STP 개념: L2 루프가 왜 치명적인가 (브로드캐스트 스톰, MAC 플래핑) — 설정은 하지 않고 필요성만 이해
- 실습: pc1↔pc2 직접 연결 → 통신 / sw1 삽입 → MAC 테이블이 채워지는 과정 관찰
- 패킷: ARP Request(브로드캐스트) → ARP Reply(유니캐스트) 캡처
- **핵심 실험**: MAC 테이블을 비우고 첫 패킷이 "플러딩"되는 것을 tcpdump로 3개 포트에서 동시 확인
- 장애 주입: 링크 down / 중복 MAC
- **과제**: "왜 스위치는 처음엔 모든 포트로 뿌리는가"를 캡처 파일로 증명

### M2. IP 주소와 L3의 등장 (4h)  → C1, C2, C5~C8, B3, B5, D1
- 개념: IPv4 주소 구조, 서브넷 마스크, 네트워크/브로드캐스트 주소, CIDR, 게이트웨이
- **사설 IP vs 공인 IP (개념 학습)** — 랩에 별도 구현 없이 지식으로 다룸
  - IPv4 주소 고갈 → 사설 주소(RFC1918: `10/8`, `172.16/12`, `192.168/16`)의 등장
  - 사설 IP는 **인터넷에서 라우팅되지 않는다** → 밖으로 나가려면 NAT가 필요 (M9로 연결)
  - 공인 IP는 어떻게 받는가 (ISP / RIR·APNIC / 우리 회사 공인 대역 확인법)
  - 특수 대역도 함께: 루프백 `127/8`, Link-local `169.254/16`(DHCP 실패 시 이 주소가 붙으면 무엇을 의심할지 → 실무 진단 단서), CGNAT `100.64/10`
  - **실무 리스크**: 사설 대역은 조직마다 겹친다. `192.168.0.0/24`를 쓰면 VPN 연동·회사 합병 시 충돌한다 → 설계 단계에서 **잘 안 쓰는 대역을 고르는 것**이 엔지니어의 일
- 현재 랩 주소가 사설/공인 중 무엇인지, 왜 그렇게 골랐는지 `ipam.yml`에서 확인 (개념을 랩 설계와 연결)
- DHCP: DORA 4단계, 릴레이가 필요한 이유 (브로드캐스트는 라우터를 넘지 못한다 → M1과 연결)
- MTU 개념 소개: 프레임 크기 제한과 프래그멘테이션 (`ping -s`로 체감)
- **L2 vs L3 결정적 차이**: 같은 대역 통신에서는 목적지 MAC = 목적지 PC / 다른 대역 통신에서는 목적지 MAC = 라우터. IP는 그대로, MAC만 바뀐다 → 이걸 tcpdump로 직접 보여준다 (이 랩 전체에서 가장 중요한 순간)
- 실습: VLAN 10/20 분리 → 통신 단절 확인 → r1을 게이트웨이로 투입 → 복구
- 서브네팅 연습 문제 세트
- 장애 주입: 잘못된 서브넷 마스크 / 게이트웨이 오설정 / VLAN 미할당 / Native VLAN 불일치
- **과제**: 주어진 요구사항(사이트 2곳, 부서 4개, 호스트 수 제각각, 30% 증설 여지)에 맞는 **요약 가능한 IP 주소 설계안**을 문서로 작성한 뒤 랩에 적용 — *이 과제가 C2(핵심 역량)의 1차 훈련*
  - 설계안에 **선택한 사설 대역과 그 선정 근거**(중복 회피 관점)를 반드시 기술

### M3. 라우팅 기초 — 정적 라우팅 (3h)  → D2, D3, D4, H5
- 개념: 라우팅 테이블 읽는 법, Longest Prefix Match, 다음 홉, 기본 경로(0.0.0.0/0), 메트릭/AD
- 실습: r1-r2-r4 구간 정적 라우팅으로 Site-A ↔ Site-B 통신 완성
- **핵심 실험**: 편도 경로만 설정 → ping 실패 → "돌아오는 길"이 없다는 것을 tcpdump로 확인 (비대칭 경로/리턴 트래픽 개념)
- traceroute 동작 원리(TTL 증가 + ICMP Time Exceeded)와 **한계**: ICMP 차단 구간에서 `* * *`가 곧 장애를 뜻하지 않는다 / 비대칭 경로는 편도만 보여준다 → **결과 오독 방지**
- 장애 주입: 리턴 경로 누락, 라우팅 루프(TTL 소진 관찰)
- **과제**: 라우팅 테이블만 보고 특정 목적지의 전체 경로를 예측한 뒤 traceroute로 검증

### M4. 동적 라우팅 — OSPF (4h)  → D5, D6, D7, C5
- 개념: 정적 라우팅의 한계, 라우팅 프로토콜의 역할, IGP vs EGP, OSPF Neighbor/LSA/Cost, 수렴(convergence)
- 실습: r1~r4에 OSPF 설정 → 이중 경로 형성
- **핵심 실험**: 통신 중 주 링크를 끊고 `ping` 손실 개수 + 경로 변경 시점을 tcpdump/traceroute로 측정. cost 조정으로 경로 유도
- BGP는 개념만 소개 (AS, 인터넷 경로, 왜 사내에선 잘 안 쓰는지)
- **Neighbor가 안 맺힐 때 점검 순서 4가지**를 체크리스트로 암기: ① Area ID ② 서브넷·마스크 ③ MTU ④ 인증·Hello/Dead 타이머
- 장애 주입: Area 불일치 / 인증 불일치 / **MTU 불일치**(Exchange 상태에서 멈춤 — C5와 연결)
- **과제**: "Site-A→Site-B 트래픽을 r3 경유로 바꾸되 장애 시 r2로 넘어가게" 설계·구현

### M6. IPv6 (3h)  → C3, C4, C9
- 개념: 왜 IPv6인가, 128bit 표기법, 축약 규칙, 주소 유형(Link-local/GUA/ULA/Multicast), Prefix
- **IPv4와의 대조 학습**: ARP ↔ NDP(NS/NA), DHCP ↔ SLAAC, 브로드캐스트 없음
- **사설/공인 개념의 IPv6 대응 (개념 학습)**: 사설 IP ↔ ULA(`fd00::/8`), 공인 IP ↔ GUA(`2000::/3`).
  IPv6는 주소가 충분해 **모든 단말이 공인 주소를 가질 수 있고 NAT가 기본적으로 필요 없다** → "그럼 보안은 무엇이 담당하는가?"(방화벽) 로 M9와 연결.
  *NAT를 보안 장치로 오해하는 흔한 오류를 여기서 교정한다.*
- 실습: 전 구간 dual-stack 구성, IPv6 정적 라우팅 + OSPFv3
- 패킷: ICMPv6 NS/NA, RA 캡처해서 ARP와 비교
- **과제**: `ping6` / `traceroute6` 로 IPv6 경로 검증, IPv4와 다른 경로가 나오게 만들어보기

### M7. 전송 계층 — 포트와 세션 (3h)  → E1~E5, A3
- 개념: TCP vs UDP, 포트 번호(well-known/ephemeral), 소켓 4-tuple, 3-way handshake, 4-way termination, TCP 상태 머신, 시퀀스/ACK, 재전송, 윈도우
- **"세션"이 무엇인지 명확히**: 방화벽/로드밸런서가 말하는 세션 = 5-tuple 상태
- 실습: `nc`로 임의 포트 서버 열기, `ss -tanp`로 상태 변화 실시간 관찰, `telnet`으로 포트 열림 확인
- **핵심 실험 — 연결 실패 증상 4종 감별표 만들기**: ① 경로 없음 ② 방화벽 DROP(무한 타임아웃) ③ REJECT·포트 미개방(즉시 Connection refused) ④ 서비스 행(연결은 되나 무응답). 각 상황을 직접 만들고 증상을 표로 정리 → *실무 최고빈도 문의에 대한 감별 도구*
- 장애 주입: 포트 미개방, 서비스 다운, TIME_WAIT 고갈
- **과제**: 하나의 HTTP 요청에서 발생하는 TCP 세션 전체 생애주기를 그림으로 그리고 패킷으로 대응시키기

### M8. 패킷 분석 — tcpdump 심화 (3h)  → H1~H4, A2
- 개념: **캡처 지점 선정이 실력의 본질** (어디서 떠야 하는가), BPF 필터 문법, `-nn -e -X -w`, snaplen, 링 버퍼
- 필터 실전: `host`, `net`, `port`, `tcp[tcpflags]`, `vlan`, `icmp`, 논리 연산 조합
- 실습: 여러 노드에서 동시 캡처 → 패킷이 사라지는 구간 특정 (**"어디까지 갔나" 추적법**)
- pcap 파일로 저장 → Wireshark로 열어 Follow TCP Stream / IO Graph
- **과제**: 미리 준비된 장애 pcap 3개를 분석해 원인 진단 리포트 작성

### M9. 응용 계층 — HTTP / DNS / FTP (4h)  → F1~F5, G4
- **DNS**: 이름 해석 과정, A/AAAA/CNAME, `dig`로 단계별 확인, "DNS 문제인지 네트워크 문제인지" 구분법
- **HTTP**: 요청/응답 구조, 메서드, 상태 코드, 헤더, Keep-Alive, telnet으로 HTTP 수동 요청 보내기(가장 교육 효과 높음), curl -v 해석, HTTPS와의 차이(TLS handshake 캡처)
- **telnet vs SSH**: 같은 로그인을 두 방식으로 하고 캡처 비교 → 평문 노출 확인. **telnet은 앞으로 "포트 진단 도구"로만 쓴다**는 실무 규범 확립
- **FTP**: 제어 채널(21)과 데이터 채널의 분리, **Active vs Passive 모드** — Active는 서버가 클라이언트로 역방향 접속을 시도한다는 사실을 tcpdump로 증명. 이것이 M9의 NAT/방화벽 이야기로 자연스럽게 연결됨
- **과제**: telnet만으로 HTTP GET 요청 수행 + FTP Active/Passive 각각의 패킷 흐름도 작성

### M10. NAT와 방화벽 (3h)  → G1~G3, E3, E4, F5, C7
- 개념: SNAT/DNAT/PAT, 포트 포워딩, 상태 기반(stateful) 방화벽, 정책 방향성
- **M2에서 개념으로 배운 사설/공인 IP를 여기서 눈으로 확인**: 사설 IP 패킷이 edge를 지나며 공인 IP로 바뀌는 순간을 edge 양쪽에서 동시 캡처 → *"인터넷에서 라우팅되지 않는다"는 말의 실제 의미*
- **흔한 오해 교정**: NAT는 보안 기능이 아니다. 우연히 inbound가 막힐 뿐이며, 실제 통제는 방화벽 정책이 한다 (M5의 IPv6 논의와 연결)
- 실습: edge 노드에 NAT 구성 → 내부에서 외부로 통신, 외부→내부 web 서버 DNAT 공개
- **핵심 실험**: NAT 환경에서 FTP Active 모드가 왜 깨지는지 확인 → Passive로 전환하면 왜 되는지 확인
- 장애 주입: 정책 순서 오류, 단방향 정책, NAT 미적용 리턴 트래픽
- **과제**: "외부에서 web은 되는데 ftp는 안 된다" 상황의 원인 규명

### M11. 트러블슈팅 방법론 + 캡스톤 (4h)  → I1~I6
- **방법론**:
  - 계층별 접근 (Bottom-up / Top-down / Divide-and-Conquer)
  - 표준 3-step: ① 물리·링크 살아있나 ② L3 도달하나(ping/traceroute) ③ 포트·서비스 응답하나(telnet/curl)
  - 범위 좁히기: 되는 것과 안 되는 것의 경계 찾기 (누가/어디서/언제부터/전부인가 일부인가)
  - 증거 기반 보고: 추측 금지, 캡처와 명령어 출력으로 말하기
  - **변경 관리**: 변경 전 상태 백업 → 롤백 계획 수립 → 작업 → 검증. "고치려다 더 크게 만드는" 사고 방지
  - 장애 보고서 템플릿 (현상 / 영향 범위 / 타임라인 / 원인 / 조치 / 재발 방지)
  - 문서화: 토폴로지도 · IP 관리대장 · 변경 이력
- **캡스톤**: 랩에 5~7개의 장애를 무작위 주입한 상태로 제공. 제한 시간 내 복구 + 원인 보고서 작성
  - 예: VLAN 오할당, 서브넷 마스크 오류, OSPF 인접 실패, 라우팅 리턴 경로 누락, 방화벽 정책, DNS 레코드 오류, MTU 문제
- **최종 산출물**: ① 트러블슈팅 체크리스트 1페이지(본인 작성) ② 장애 보고서 ③ 최종 네트워크 구성 문서
- **통과 기준**: 1.3절 졸업 판정 조건 참조 (복구 과정에서 새 장애를 만들지 않을 것 포함)

---

## 5. 모듈 문서 표준 포맷 (M0·M1 에서 확정)

```
modules/mXX-<slug>/
├── meta.yml        모듈 메타 (id · title · stage · duration · competencies · objectives)
├── README.md.j2    개념 + 실습 지시서          → 교육생
├── tasks.md.j2     과제 + 확인 문제            → 교육생
├── answers.md.j2   정답 · 해설 · 진행 주의사항  → 교육 담당자 전용
├── assessment.yml  **퀴즈 + 랩 상태 검사 + 서술 과제** (6장)
└── verify.sh       자동 검증 (playbooks/verify.yml 호출)
```

**`.j2` 인 이유**: 교재에도 주소를 하드코딩하지 않는다.
`config/site.yml` 이 바뀌면 교재 안의 IP·MAC 도 따라 바뀐다 (`make modules`).
템플릿에서 쓸 수 있는 값은 `tools/labdesign.py` 의 `doc_context()` 참조.

**README.md 구성**

| 절 | 내용 |
|---|---|
| 학습 목표 | `meta.yml` 의 objectives (역량 코드 병기) |
| 사전 지식 체크 | 이전 모듈 복습 질문 3개 |
| 이 단계의 랩 | Mermaid 토폴로지(그 시점) + 노드·주소 표 |
| 개념 | 표·그림 중심. 각 개념 끝에 **"그래서 실무에서는"** 한 줄 |
| 실습 | 복붙 가능한 명령 + **예상 출력** + 관찰 포인트 |
| 관찰 포인트 정리 | "무엇을 어떤 명령으로 보고, 정상이면 어떻게 보이는가" 표 |
| 장애 주입 챌린지 | `make break SCENARIO=mXX-NN` — **복구 전에 원인을 말한다** |
| 흔한 오해 · 실무 팁 | 오해/사실 표 + 실장비 CLI 대응표 |

**meta.yml 은 웹 콘솔(Step 5)의 데이터 소스**이기도 하다.
`dist/modules/index.yml` 로 모아져 모듈 목록·진도 표시에 쓰인다.

---

## 6. 모듈 평가 체계 — 퀴즈 · 검사 · 서술 · 통과 게이팅

과제를 "내주고 끝"내지 않는다. **개념을 아는지(퀴즈)** 와 **실제로 되게 만들었는지(검사)** 를
각각 확인하고, **둘 다 통과해야 다음 모듈이 열린다.**

여기에 세 번째 축이 있다. **왜 그렇게 판단했는지(서술)** 는 기계가 채점할 수 없다.
그렇다고 관리자가 매번 봐야 진행된다면, 회사에서 돌리는 교육에서는 그것이 곧 병목이 된다.
그래서 **자동으로 거를 수 있는 것은 자동으로 거르고, 사람은 걸러지지 않은 것만 본다** (6.7).

### 6.1 세 축

| | 퀴즈 | 랩 상태 검사 | 서술 과제 |
|---|---|---|---|
| 확인하는 것 | 개념·판단 (왜 그런가) | 설정이 들어갔는가 / 의도대로 통신되는가 | 진단 과정과 근거 |
| 방식 | 객관식(단일·복수) · 단답 | 실제 노드에서 명령을 돌려 출력을 판정 | 자유 서술 · 글자 구성도 |
| 채점 | 서버에서. **정답과 해설은 클라이언트로 나가지 않는다** | 통과/실패 + 실패 이유 + 힌트 | 루브릭 자동 사전검토 → 필요할 때만 사람 |
| 통과 기준 | 기본 80% | **전 항목 통과** | **제출**(기본) / 관리자 승인(캡스톤) |
| 재시도 | 무제한. 틀린 문항만 해설 공개 | 무제한 | 무제한 |
| 진행을 막는가 | ○ | ○ | **✗** (캡스톤만 ○) |

**모든 모듈에 둘 다 넣지 않는다.** 모듈 성격에 맞춰 비중을 조절한다
(M7·M8 은 퀴즈 비중이 크고, M2·M4·M10 는 검사 비중이 크다).

### 6.2 검사 정의 — 선언형

`modules/mXX/assessment.yml` 에 적으면 `tools/run-checks.py` 가 실행한다.
주소는 `{{ ip.pc2 }}` 처럼 쓰면 `config/site.yml` 기준으로 치환된다 (교재와 동일한 규칙).

```yaml
checks:
  items:
    - id: same-subnet-ping
      kind: connectivity          # config | connectivity | state
      title: "pc1 → pc2 통신이 되는가"
      host: pc1
      run: "ping -c2 -W2 {{ ip.pc2 }} >/dev/null && echo REACHABLE"
      expect: "REACHABLE"
      hint: "링크 → 주소 → 스위치 포트 순서로 확인할 것"
```

판정: `expect`(포함) · `expect_all`(모두 포함) · `expect_none`(없어야 함) · `expect_regex` · `expect_rc`.
검사는 **기본적으로 root 권한 없이** 돈다. 필요한 항목만 `become: true` 를 적는다.

> 구현 메모: 검사 결과는 각 태스크가 **컨트롤러에 JSON 파일로 직접 쓴다.**
> ansible 표준 출력을 파싱하면 형식이 바뀔 때 조용히 깨진다.

### 6.3 버튼 이름 (확정안)

| 버튼 | 이름 | 왜 |
|---|---|---|
| 제출 | **제출하고 검증** | 한 번에 무슨 일이 일어나는지(채점+검사)가 이름에 드러난다 |
| 통과 후 | **다음 모듈 →** | 다음 행동을 지시한다 |
| 모듈 상태 맞추기 | **이 모듈 적용** | 단계를 올리는 것과 되돌리는 것이 같은 동작이다 (reset.yml 이 site.yml 을 import 한다). 버튼을 둘로 나눠 두었더니 앞선 랩에서 "적용"을 눌러 반쪽만 되돌아가는 사고가 났다 — 하나로 합쳤다 |
| 랩 수명주기 | **랩 생성** / **랩 삭제** | Terraform 수준임을 구분 |
| 연결 확인 | **연결 확인** | 평가와 무관한 단순 도달성 점검 |

*대안*: 「제출」+「검증」 분리, 「채점하기」, 「확인」. 분리하면 두 번 눌러야 해서
"제출했는데 왜 통과가 안 되지" 혼란이 생긴다. **하나로 합치는 쪽을 권장한다.**

### 6.4 통과 게이팅

```
M0 [퀴즈 ✔ + 검사 ✔] ──열림──> M1 [퀴즈 ? + 검사 ?] ──잠김──> M2 🔒
```

- 앞 모듈을 통과해야 다음 모듈 문서가 열린다 (목록에서 🔒, 직접 URL 접근도 403)
- **관리자는 전부 열려 있다** (교재 검토·시연용)
- 첫 화면은 **열려 있는 모듈 중 마지막** = "지금 해야 할 모듈"
- 초기화·장애 주입으로 랩을 되돌려도 **점수와 이력은 지워지지 않는다**

### 6.5 이력

`attempts`(제출 이력) · `progress`(모듈별 진도) 테이블에 남긴다.

| 볼 수 있는 사람 | 범위 |
|---|---|
| 본인 | 자기 이력 (`/history`) |
| 관리자 | 전 교육생 (`/history?who=<계정>`) |

이력에는 **제출 시각 · 점수 · 통과 여부 · 틀린 문항 · 실패한 검사 항목**이 남는다.
누가 어디서 막혔는지 보이므로 교재를 고칠 근거가 된다.

### 6.6 모듈별 평가 설계

> 표기: 퀴즈는 주제, 검사는 `[설정]` `[통신]` `[상태]` 로 종류를 표시.
> ★ 는 그 모듈에서 가장 중요한 확인 항목.
> 서술 열의 `자동` 은 루브릭 충족 시 관리자 검토 없이 통과, `검토` 는 큐로 올라가지만 진행은 막지 않음,
> `승인` 은 관리자 승인이 있어야 통과 (6.7).

| 모듈 | 퀴즈 (개념) | 랩 상태 검사 | 서술 |
|---|---|---|---|
| **M0** 오리엔테이션 | 관리망 IP 로 한 검증이 왜 무효인가 ★ · eth1/mgmt0 down 시 SSH · 콘솔 복구 · ping/telnet/traceroute/tcpdump 용도 구분 | `[상태]` 전 노드 접속 · pc1·pc2 링크 up (실습에서 내린 것을 되돌렸는가) | — *(오리엔테이션은 가볍게)* |
| **M1** L2·스위칭 | `NO-CARRIER` 판독 ★ · 플러딩 조건 2가지 · ARP Request/Reply 방향 · `ip neigh` 로 알 수 **없는** 것 · L2 루프가 위험한 이유 · 브로드캐스트 도메인 수 | `[상태]` 링크 up · `[통신]` 같은 대역 ping · `[상태]` sw1 이 두 MAC 을 학습했는가 ★ · `[설정]` 브리지 포트 · `[설정]` 실습용 정적 ARP 를 정리했는가 | 장애 원인 서술 `자동` · L2 구성도 `검토` |
| **M2** IP·L3 등장 | 서브네팅 계산 ★ · 게이트웨이의 역할 · VLAN 분리 후 왜 끊기는가 · **다른 대역 통신 시 목적지 MAC** ★ · 사설/공인 구분 · DHCP DORA · `169.254` 가 뜻하는 것 | `[설정]` r1 서브인터페이스와 VLAN 게이트웨이 주소 · `[설정]` sw1 트렁크 태그(10,20)+native · `[설정]` pc 기본 경로가 게이트웨이인가 · `[통신]` pc1 → pc2 다른 대역 ping ★ | 주소 설계 근거 `자동` · 설계 산출물 형식 검증은 자동화 · 전체 구성도 `검토` |
| **M3** 정적 라우팅 | 라우팅 테이블로 다음 홉 예측(LPM) ★ · 리턴 경로가 없으면 생기는 일 ★ · AD vs 메트릭 · traceroute 의 `* * *` 오독 | `[설정]` r1·r2·r4 에 상대 사이트 요약 경로가 있는가 · `[통신]` pc1 → web ping · `[통신]` pc1 → web HTTP 200 ★ · `[상태]` traceroute 홉 수가 설계와 맞는가 | 리턴 경로를 어떻게 확인했는가 `자동` |
| **M4** OSPF | Neighbor 미형성 원인 4가지 ★ · cost 와 경로 선택 · 수렴이란 무엇인가 · IGP/EGP 구분 · default-information originate | `[설정]` 정적 경로가 사라지고 OSPF(`O`) 경로로 학습되는가 ★ · `[상태]` 전 라우터 Neighbor Full · `[설정]` 백업 링크 cost · `[통신]` 주 경로가 r2 경유인가(traceroute) | 정적→OSPF 전환 시 무엇이 달라졌는가 `자동` |
| **M6** IPv6 | 축약 규칙 · ULA/GUA ↔ 사설/공인 대응 ★ · NDP 와 ARP 대조 · IPv6 에 NAT 가 필요 없는 이유 ★ | `[설정]` 전 구간 IPv6 주소 배정 · `[상태]` OSPFv3 Neighbor · `[통신]` `ping6` 사이트 간 · `[통신]` dual-stack 동시 동작 | — |
| **M7** TCP·포트·세션 | **연결 실패 4종 감별** ★★ (경로없음/DROP/REJECT/서비스행) · TCP vs UDP 선택 · 3-way·4-way · 상태 전이 · well-known/ephemeral | `[통신]` web 80 포트 응답 · `[상태]` `ss` 로 ESTABLISHED 세션 확인 · `[상태]` 닫힌 포트 접속 시 즉시 거부인가 *(퀴즈 비중 큰 모듈)* | 연결 실패 4종 감별 근거 `자동` ★ |
| **M8** tcpdump | BPF 필터 작성 ★ · **캡처 지점 선정** ★★ · `-e`/`-n`/`-w` 의미 · `not port 22` 를 넣는 이유 | `[상태]` 과제로 지정한 pcap 이 실제로 생성됐고 조건에 맞는 패킷을 담고 있는가 ★ *(`tcpdump -r` 로 검증)* | 캡처 지점을 왜 거기로 잡았는가 `자동` ★ |
| **M9** HTTP·DNS·FTP | **DNS 실패인가 네트워크 실패인가** ★★ · 상태 코드의 책임 소재 · FTP Active/Passive 연결 방향 ★ · HTTPS 가 추가하는 것 | `[설정]` resolv.conf · `[통신]` `dig` 이름 해석 · `[통신]` HTTP 200 + `X-Lab-Node` 헤더 · `[통신]` FTP 220 배너 · `[통신]` Passive 전송 성공 ★ | DNS 실패/네트워크 실패 판별 과정 `자동` |
| **M10** NAT·방화벽 | SNAT/DNAT/PAT 구분 · stateful 이 리턴 트래픽을 여는 원리 ★ · FTP Active 가 NAT 에서 깨지는 이유 ★ · **NAT 는 보안 기능이 아니다** ★ | `[통신]` pc1 → 외부 사이트 HTTP 200 (SNAT) ★ · `[통신]` 외부 → web 공개 IP HTTP 200 (DNAT) · `[설정]` nft 규칙에 snat/dnat 존재 · `[상태]` conntrack 세션 · `[통신]` **사설 IP 로는 외부에서 닿지 않는가** (음성 검사) ★ | 장애 원인 서술 `자동` |
| **M11** 캡스톤 | 트러블슈팅 순서 선택 · 영향 범위를 먼저 규정하는 이유 ★ · 증거 기반 보고 | **M1~M10 검사 전체를 한 번에** — 장애가 주입된 상태에서 시작해 전부 통과시켜야 한다 ★★ | **최종 보고서 `승인`** ★★ · 최종 구성도 `승인` |

**설계 의도**

- 퀴즈는 "외웠는가"가 아니라 **"판단할 수 있는가"** 를 묻는다. 대부분 상황 제시형이다.
- 검사에는 **뒷정리 항목**을 일부러 넣는다 (M1 의 정적 ARP 정리).
  실습을 원래 상태로 되돌리는 것까지가 작업이라는 감각을 심는다.
- M10 의 **음성 검사**(되면 안 되는 것이 안 되는가)는 다른 모듈에도 확장할 수 있다.
  "되는 것"만 확인하면 절반만 본 것이다.
- M11 은 앞 모듈 검사 항목을 재사용한다. 그래서 **각 모듈의 검사를 잘 써두면 캡스톤이 저절로 만들어진다.**
- 서술은 **모든 모듈에 넣지 않는다.** 같은 형식의 글을 10번 쓰면 형식만 남는다.
  진단 과정을 말로 정리할 가치가 있는 모듈에만 둔다 —
  M1 · M2 · M3 · M4 · M7 · M8 · M9 · M10 와 캡스톤(M11). **M0 과 M6 에는 없다.**
  M1 · M2 는 구성도까지 두 개이고(`auto_pass: false`), M11 은 세 개다(체크리스트 · 보고서 · 구성 문서).

---

### 6.7 서술 과제와 검토 정책 — 관리자를 병목에 두지 않는다

교육 기관이 아니라 **회사**에서 돌리는 교육이다. 관리자(교육 담당자)에게는 본업이 있고,
"과제를 봐 줘야 교육생이 다음으로 넘어가는" 구조는 반드시 밀린다. 그래서 두 가지를 분리했다.

```
        제출
          │
          ├── 퀴즈 ─────────── 자동 채점 ────────────┐
          ├── 랩 상태 검사 ──── 실제 노드에서 판정 ────┼──> 통과하면 다음 모듈이 열린다
          │                                          │
          └── 서술 과제 ─┬─ 루브릭 충족 → 자동 통과 ───┘   (관리자는 보지 않는다)
                         │
                         └─ 미충족·구성도 → 검토 큐 ────> 관리자가 나중에 몰아서 본다
                                                          (진행은 막지 않는다)
```

**규칙**

| # | 규칙 | 이유 |
|---|---|---|
| A1 | 서술 과제는 **제출하면 통과** (`review: optional`) | 교육생이 관리자를 기다리지 않는다 |
| A2 | 루브릭을 전부 충족하면 **자동 통과**, 검토 큐에 뜨지 않는다 | 사람은 **자동으로 못 거른 것만** 본다 |
| A3 | 구성도 등 자동 채점이 무의미한 것은 `auto_pass: false` | 큐에는 올리되 진행은 막지 않는다 |
| A4 | **캡스톤(M11)만 `review: required`** — 승인이 있어야 통과 | 졸업 판정은 사람이 한다 |
| A5 | 재제출 요청에는 **피드백 필수** | "다시 내"만 받으면 무엇을 고칠지 모른다 |
| A6 | 관리자가 요청한 재제출은 화면에 뜨지만 **진행을 되돌리지 않는다** | 이미 넘어간 교육생을 뒤에서 잡아채지 않는다 |

**자동 사전검토(루브릭)** 는 채점이 아니다. 단어가 있는지 훑는 것뿐이고,
관리자가 **20초 만에 판단할 수 있게** 앞에 요약을 붙여 주는 장치다. 화면에도 그렇게 적어 둔다.

```yaml
written:
  items:
    - id: cause
      title: "장애 원인 서술 — 어떻게 원인에 도달했는가"
      review: optional        # 제출하면 통과
      auto_pass: true         # 루브릭을 다 채우면 관리자가 볼 필요 없다
      min_chars: 120
      rubric:
        - {key: 근거, desc: "명령 결과로 근거를 댔는가", any: ["ip link", "tcpdump", "bridge"]}
        - {key: 조치, desc: "고치고 되돌렸는가",         any: ["복구", "원복", "삭제"]}
```

교육생에게도 같은 루브릭이 **다뤄야 할 것**으로 미리 보인다. 감점 기준을 숨기지 않는다.
빠진 항목은 제출 직후 "**근거** 미언급"으로 돌려주므로, 대부분은 관리자에게 가기 전에 스스로 고친다.

**관리자가 반드시 보는 것 (자동화 대상에서 제외)**

| 항목 | 어디에 | 왜 사람이 보는가 |
|---|---|---|
| 캡스톤 최종 보고서 | M11 (`review: required`) | 졸업 판정. 진단의 **논리**는 기계가 못 본다 |
| 장애 원인 서술 | M1·M3·M7·M10 등 (`optional` + 루브릭) | 대부분 자동 통과되고, 부실한 것만 올라온다 |
| 토폴로지 구성도 | M1·M2·M11 (`auto_pass: false`) | 그림은 자동 채점이 무의미하다 |

IP 주소 설계 산출물(M2)은 **형식 검증을 자동화**한다 — 겹침·용량·게이트웨이 위치는
`tools/validate-site.py` 와 같은 규칙으로 판정할 수 있다. 사람은 "왜 이렇게 잘랐는가"만 본다.

**검토 큐** (`/admin/reviews`)

- 각 (교육생·모듈·항목)의 **최신 제출본만** 올라온다. 재제출하면 이전 것은 큐에서 사라진다
- 자동 사전검토 결과가 항목별 ✔/✘ 로 먼저 보인다
- 승인 / 재제출 요청 두 버튼. 재제출 요청은 피드백을 적지 않으면 저장되지 않는다
- 헤더에 대기 건수가 뜬다. **0 이면 아무것도 하지 않아도 된다**

---


### 6.8 캡스톤 시험 세션 — 제한 시간과 점수 동결

캡스톤(M11)은 "제한 시간 안에 고쳐라"가 성립해야 한다. 그런데 **끝났을 때 랩을 못 만지게
막는 것은 이 구조에서 성립하지 않는다.** 교육생은 점프 호스트를 거쳐 노드에 SSH 로 직접
붙고, 그 경로는 콘솔을 지나지 않는다. 게다가 랩 계정은 `frr`·`frrvty` 그룹에 들어 있어서
sudo 를 회수해도 `vtysh` 로 라우터를 고칠 수 있다.

그래서 **막는 대신 소용없게 만든다.**

```
      제한 시간 ───────────────────────────▶ 마감
                                              │
                            ┌─────────────────┴──────────────────┐
                            │                                    │
                     ① 점수 동결                            ② 콘솔 잠금
              그 순간의 검사 결과를 확정본으로          실행 계열 작업을 거부한다
              못 박는다. 이후 무엇을 고쳐도            (verify 만 남긴다 — 보고서를
              성적은 바뀌지 않는다                     쓰려면 상태를 봐야 하므로)
                            │                                    │
                       본체 · 우회 불가                    사고 방지 장치
```

**규칙**

| # | 규칙 | 이유 |
|---|---|---|
| B1 | 시나리오는 **서버가 무작위로 고른다**. 교육생에게 보이지 않는다 | 고를 수 있으면 답을 아는 채로 시작한다 |
| B2 | 주입이 **끝난 뒤에** 시계가 간다 | 준비 시간(수 분)을 시험 시간에 넣으면 불공정하다 |
| B3 | 진행 중에는 `verify`·`check` 만 허용 | 랩은 터미널에서 고치는 것이다. 버튼으로 고치면 시험이 아니다 |
| B4 | 마감 시각이 지나면 확정 전에도 `check` 를 막는다 | 확정 직전 몇 초에 끼어들어 `passed_at` 을 만드는 것을 차단 |
| B5 | 마감 뒤에는 `verify` 만 허용 | 상태는 봐야 한다 — 인계 보고서의 재료다 |
| B6 | **관리자는 잠금의 예외** | 다음 응시를 준비해야 한다. 대신 모든 조작이 이력에 남는다 |
| B7 | 마감 확정은 **스위퍼 한 경로**로만 일어난다. 관리자의 [즉시 마감]도 마감 시각을 당길 뿐이다 | 경로가 둘이면 결과가 어긋난다 |
| B8 | 콘솔이 꺼져 있던 동안 지난 마감도 **기동 직후** 확정된다 | 마감이 프로세스 생존에 의존하면 "껐다 켜서 시간을 번다"가 가능해진다 |
| B9 | 검사를 못 돌려도 **마감은 반드시 한다** | 안 그러면 잠금이 안 걸린 채 시험이 영원히 열려 있다 |
| B10 | **재응시 무제한.** 새 세션을 열면 이전 마감이 효력을 잃는다 | 6.1 의 재시도 원칙과 같다. 이전 확정본은 이력으로 남는다 |
| B11 | 마감 뒤에는 **주입된 장애를 공개**한다 | 종료 후 정답 공개가 학습의 절반이다 |

**못 고쳐도 낸다.** 시간이 끝나면 랩 검사는 못 돌리지만 서술 제출은 그대로 저장된다
(HTTP 200, 오류가 아니라 정상 경로). 남은 것을 **인계 보고서**에 적는 것까지가 과제다 —
"고쳤습니다"는 누구나 쓰지만 *"이 세 가지는 배제했고, 남은 후보는 둘이며, 다음 사람은
여기서 시작하면 된다"* 는 실제로 진단한 사람만 쓸 수 있다. 그 틀 자체가 채점 장치다.

**설정** (`config/site.yml` → `console.capstone`)

| 키 | 기본값 | 뜻 |
|---|---|---|
| `module` | `m11` | 시험 세션을 쓰는 모듈 |
| `minutes` | `45` | 제한 시간 |
| `faults` | `3` | 무작위로 주입할 장애 개수 |
| `sweep_seconds` | `20` | 마감 확정을 시도하는 주기 |
| `pool` | `[]` | 비우면 `scenarios/` 전체에서 고른다 |

> **한계를 문서에 남긴다.** 콘솔 잠금은 정책적 통제이지 기술적 봉쇄가 아니다.
> 완전한 물리 잠금이 필요하면 점프 호스트에서 관리망을 끊어야 하고
> (`nft ... meta skuid <교육생 uid> ip daddr <관리망> drop`), 그것은 교육생 OS 계정이
> 개별이어야 성립한다. 지금은 공유 계정(`trainee`)이므로 성립하지 않는다.
> **성적은 확정본으로만 판단할 것.**


## 7. 플랫폼 아키텍처 — Proxmox VE + AWS 이식성 설계

### 7.1 핵심 전략: 토폴로지는 "데이터", 플랫폼은 "렌더러"

```
              ┌──────────────────────────────────────────┐
              │   Single Source of Truth (플랫폼 무관)     │
              │   topology.yml  : 노드 / 링크 / 역할        │
              │   ipam.yml      : IPv4·IPv6·VLAN 설계      │
              │   configs/*.j2  : 노드 설정 템플릿           │
              └───────────────────┬──────────────────────┘
                                  │
      ┌───────────────┬───────────┴─────────┬────────────────────┐
      ▼               ▼                     ▼                    ▼
  Proxmox         EVE-NG              AWS (Nested)        AWS (Native VPC)
  Terraform       REST API /          EC2 1대 안에         VPC / Subnet /
  + Linux Bridge  .unl 임포트          랩 전체 재현          Route Table / TGW
      │               │                     │                    │
      └───────────────┴───────────┬─────────┴────────────────────┘
                                  ▼
                    Ansible (SSH로 동일 설정 적용)
                    → 플랫폼이 달라도 노드 설정은 100% 동일
```

**이식성의 열쇠: 노드 설정을 플랫폼 기능이 아니라 SSH로 넣는다.**
플랫폼마다 다른 것은 "배선(링크 생성)"뿐이고, 그 위의 IP/라우팅/서비스 설정은 Ansible이 어디서나 똑같이 적용한다. 이 원칙만 지키면 Proxmox → EVE-NG → AWS 이동 비용이 거의 0이 된다.

### 7.2 AWS 이식 시의 근본 제약 (반드시 인지)

| 항목 | 온프렘 (Proxmox / EVE-NG) | AWS VPC |
|---|---|---|
| L2 브로드캐스트 도메인 | 있음 | **없음** |
| ARP 관찰 | 전 구간 가능 | 게이트웨이 ARP 정도만 |
| VLAN / 트렁크 | 가능 | **불가** |
| MAC 학습 · 플러딩 | 관찰 가능 | **불가** |
| OSPF 멀티캐스트(224.0.0.5) | 정상 동작 | **불가** (터널 또는 unicast neighbor 필요) |
| 임의 라우팅 | 자유 | Source/Dest Check 해제 + Route Table 등록 필수 |
| 구간 패킷 캡처 | 어느 링크든 tcpdump | 인스턴스 내부만 자유 / 구간은 Traffic Mirroring |

→ **M1·M2(L2/스위칭)와 M4(OSPF)는 AWS VPC에 그대로 옮길 수 없다.** 대응은 두 가지.

**방식 A — Nested: EC2 1대 안에 랩 전체를 재현 (이식성 확보용, 권장)**
- 랩이 인스턴스 내부에서 돌아가므로 **커리큘럼 100% 그대로 동작**
- 컨테이너/네트워크 네임스페이스 기반이면 일반 인스턴스(m6i.2xlarge급)로 충분 — 시간당 $0.5 이하
- EVE-NG나 KVM 기반 VM 노드까지 옮기려면 `.metal` 인스턴스 필요 → 시간당 $4~5, 교육 기간만 켜는 용도
- 용도: 원격 교육, 사내 서버 없이 임시 운영, 교육생별 1인 1랩

**방식 B — Native VPC 매핑: "클라우드 네트워킹" 학습 모듈로 전환 (확장용)**
- L2가 없다는 사실 자체가 훌륭한 교보재 → **M11(신규 확장): 온프렘 vs 클라우드 네트워킹**
- 개념 1:1 대응표를 만들어 학습:
  | 온프렘 개념 | AWS 대응 |
  |---|---|
  | 브로드캐스트 도메인 / VLAN | Subnet |
  | 라우터 / 라우팅 테이블 | Route Table, Transit Gateway |
  | 스위치 · MAC 학습 | (없음) VPC가 은닉 |
  | 정적 라우팅 | Route Table 엔트리 |
  | 동적 라우팅 | TGW / Direct Connect BGP |
  | 방화벽 | Security Group(stateful) / NACL(stateless) |
  | NAT | NAT Gateway, EIP |
  | 사이트 간 연결 | VPC Peering, TGW, Site-to-Site VPN |
- 실습: **동일한 IP 설계도**를 VPC로 구현하고 "무엇이 사라졌는가 / 무엇으로 대체됐는가"를 정리

**결론: 방식 A로 이식성을 확보하고, 방식 B는 M11 확장 모듈로 추가한다.** 둘은 상충하지 않으며 오히려 세트로 가르칠 때 이해가 가장 깊어진다.

### 7.3 확정 플랫폼: Proxmox VE 단독

> 참고 — EVE-NG 대비 Proxmox 단독의 장단점
>
> | 항목 | Proxmox VE (확정) | EVE-NG |
> |---|---|---|
> | 토폴로지 배선 | Linux Bridge / SDN VNet 정의 | GUI 드래그 |
> | 자동화 · IaC | **Terraform provider 성숙, API 우수** | REST API 제한적 |
> | 스냅샷 · 초기화 | **VM 스냅샷/클론 강력** | 랩 단위 wipe |
> | 리소스 효율 | **LXC 활용 시 매우 우수** | 노드당 VM, 무거움 |
> | AWS 이식 | **cloud-init ↔ user-data 대칭, 이식 용이** | .unl 이식 불가 |
> | 토폴로지 시각화 | 약함 (7.3.2 보완책 참조) | 강함 |
> | 벤더 이미지 | 직접 준비 | 통합 지원 |
>
> Terraform 코드화 + AWS 이식성 + 리소스 효율이 목표와 정확히 일치하므로 Proxmox 단독이 타당한 선택.

#### 7.3.1 Proxmox 랩 구성 방식

```
┌─────────────────────── Proxmox VE Host ────────────────────────┐
│                                                                 │
│  [pc1]   [pc2]   [sw1]   [r1]   [r2]   [r3]   [r4]   [web] ...  │
│    │       │       │      │      │      │      │       │        │
│  ══╧═══════╧═══════╧══════╧══════╧══════╧══════╧═══════╧══      │
│         Linux Bridge (링크 1개 = 브리지 1개, 물리 NIC 미연결)      │
│         br-pc1sw1 / br-sw1r1 / br-r1r2 / br-r2r4 / ...          │
│                                                                 │
│  + vmbr0 (관리망) : 모든 노드의 eth0 = Ansible SSH 접속 경로      │
└─────────────────────────────────────────────────────────────────┘
```

**핵심 설계 규칙 3가지**

1. **링크 1개 = 격리된 Linux Bridge 1개.** 물리 NIC를 붙이지 않은 브리지를 만들면 그것이 곧 "랜선"이다. 브리지 이름에 링크 의미를 담는다(`br-r1r2`). → 이렇게 하면 `topology.yml`의 링크 정의가 브리지 목록으로 1:1 변환된다.
2. **관리망(Out-of-Band)을 분리한다.** 모든 노드는 `mgmt0`를 관리망(vmbr9)에 물리고, 랩 트래픽은 `eth1` 이후만 사용한다. 인터페이스 이름은 MAC 기준으로 고정한다. 이유:
   - 교육생이 랩 네트워크를 완전히 망가뜨려도 **접속은 유지**된다 (실무의 콘솔 서버와 동일한 개념 — 이것 자체가 교육 소재)
   - Ansible이 랩 상태와 무관하게 항상 설정을 적용/초기화할 수 있다
   - **주의**: 모든 실습·검증은 반드시 랩 인터페이스 기준으로 한다. 관리망 때문에 "되는 것처럼 보이는" 함정을 M0에서 명시적으로 가르친다.
3. **노드는 Ubuntu cloud image VM + cloud-init.** LXC보다 무겁지만
   - cloud-init(Proxmox) ↔ user-data(EC2 AMI) 구조가 대칭 → **AWS 이식 시 그대로 재사용**
   - 커널 분리로 sysctl/iptables/네임스페이스 제약이 없음 (LXC는 브리지·방화벽 실습에서 privileged 필요)
   - 스냅샷 롤백이 깔끔 → 장애 주입 실습에 유리
   - *리소스가 빠듯하면 PC/서버 노드만 LXC로 내려도 됨 (혼용 가능)*

#### 7.3.2 Proxmox 단독의 약점 보완 — "토폴로지가 눈에 안 보인다"

초보 교육생에게 이건 실제 장애물이므로 보완책을 커리큘럼에 내장한다.

- `topology.yml` 하나에서 **Mermaid 다이어그램을 자동 생성**해 각 모듈 문서 맨 위에 삽입 (그 모듈 시점의 토폴로지만)
- 교육생용 **"랩 지도" 1페이지** 상시 참조본 제공 (노드/IP/브리지/포트 전부 표기)
- 브리지·인터페이스 **명명 규칙을 엄격히** 지켜 이름만 봐도 연결이 읽히게 함
- 각 노드에 `lab-whereami` 헬퍼 스크립트 배치 → 자기 인터페이스가 어느 링크에 붙어 있는지 출력

### 7.4 확정 노드 이미지: 오픈소스 통일

| 역할 | 노드 | 이미지 | 핵심 도구 · CLI |
|---|---|---|---|
| 라우터 | r1, r2, r3, r4 | Ubuntu 24.04 + **FRRouting** | `vtysh` (Cisco IOS 호환 명령), `ip route` |
| 스위치 | sw1, sw2 | Ubuntu 24.04 + **Linux Bridge (VLAN filtering)** | `bridge fdb show`, `bridge vlan show` |
| PC | pc1, pc2 | Ubuntu 24.04 (desktop tools) | `ping`, `traceroute`, `ss`, `tcpdump`, `curl`, `dig` |
| 서버 | web / ftp / dns | Ubuntu 24.04 + nginx / vsftpd / bind9 | 서비스별 |
| 경계 | edge (M10) | Ubuntu 24.04 + **nftables/iptables + FRR** | NAT · 방화벽 |

**선택 근거**
- Proxmox · AWS 양쪽에서 **동일 이미지 · 동일 설정**으로 동작 → 이식성 목표에 직결
- 라이선스 리스크 없음
- FRR `vtysh`는 `show ip route` / `show ip ospf neighbor` / `show running-config` 등 Cisco 명령을 그대로 사용 → 실장비 전환 학습 비용이 낮음
- Linux Bridge는 MAC 학습·플러딩·VLAN 동작이 **명령어로 다 들여다보여서** 초보 교육에 오히려 유리 (`bridge fdb show`는 `show mac address-table`과 1:1 대응)
- 리소스가 가벼워 10~12노드 동시 구동 가능

**보완**: M11 완료 후 부록으로 **벤더 CLI 매핑표**(FRR ↔ Cisco IOS ↔ 회사 실장비)를 작성한다. 개념은 동일하고 명령어만 치환되므로 전환 부담이 작다.

### 7.5 AWS 이식 — 지금은 "설계만" (구현 보류)

구현은 나중에 하되, **지금 만드는 모든 산출물이 나중에 그대로 이식되도록** 아래 규칙만 지킨다.

| 규칙 | 내용 | 이식 시 효과 |
|---|---|---|
| R1 | 토폴로지·IP·설정을 `topology.yml` / `ipam.yml` / Jinja2 템플릿으로만 정의 | 플랫폼 코드와 완전 분리 |
| R2 | 노드 설정은 **전부 Ansible이 SSH로** 적용 (Proxmox 기능 사용 금지) | 설정 코드 재사용률 100% |
| R3 | Terraform을 `modules/proxmox/`, `modules/aws/` 로 분리하고 **입력 변수 스키마를 동일하게** 유지 | 백엔드 교체만으로 전환 |
| R4 | 노드 프로비저닝은 cloud-init 기반 (Proxmox cloud-init ↔ EC2 user-data) | 부트스트랩 로직 재사용 |
| R5 | 하드코딩된 IP·인터페이스명 금지, 전부 변수화 | 주소 체계 변경 흡수 |

**이식 경로 (추후 실행 시)**
- 1순위: **Nested** — EC2 1대(`m6i.2xlarge` 또는 `.metal`) 안에 KVM/컨테이너로 랩 전체 재현 → 커리큘럼 100% 보존
- 2순위: **Native VPC** — L2·OSPF는 불가하므로 M11(온프렘 vs 클라우드 비교) 확장 모듈로 전환

### 7.6 리소스 산정 (최종 12노드 기준)

| 노드 유형 | 수량 | vCPU | RAM | 디스크 |
|---|---|---|---|---|
| 라우터 (FRR) | 4~5 | 1 | 768MB | 4GB |
| 스위치 (bridge) | 2 | 1 | 512MB | 4GB |
| PC | 2 | 1 | 512MB | 4GB |
| 서버 (web/ftp/dns) | 3 | 1 | 768MB | 6GB |
| **합계** | **12** | **12 (오버커밋 가능)** | **약 8GB** | **약 55GB** |

- Proxmox 호스트 권장: **8코어 / 32GB RAM / SSD 200GB** (1인 1랩 다중 운영 시 RAM 64GB 이상)
- 최소: 4코어 / 16GB (공용 랩 1세트)
- 템플릿 1개 + linked clone으로 디스크 사용량을 크게 줄일 수 있음

---

### 7.7 공개 배포 설계 — 환경 값과 설계의 분리

이 프로젝트는 **GitHub 공개**를 전제로 만든다. 그래서 "우리 회사 값"이 저장소에 섞이지 않도록 구조로 막았다.

```
config/site.yml         공개해도 안전한 기본값 (RFC5737 문서 대역, Proxmox=localhost)  [커밋]
config/site.local.yml   실제 환경 값 (사내 대역·Proxmox 주소·SSH 키)              [git 제외]
var/runtime.yml         웹 콘솔에서 입력한 Proxmox 접속 정보 (7.9)                [git 제외]
      │  깊은 병합 (아래로 갈수록 우선)
      ▼
design/ipam.yml         "블록을 어떻게 자를 것인가"만 있다. 절대 주소 없음.
design/topology.yml     배선
design/routing.yml      경로 정책 (상징적 표기: to.zone / via.link)
      │
      ▼  tools/labdesign.py — 주소 계산은 여기서만 한다
      ▼
dist/ · inventory/ · lab.auto.tfvars.json     생성물 [git 제외]
```

**핵심 규칙**

| # | 규칙 | 이유 |
|---|---|---|
| P1 | 절대 주소는 `config/site.yml` 에만 존재 | 대역 변경이 한 줄로 끝난다 |
| P2 | 실제 환경 값은 `site.local.yml` (git 제외) | 사내 정보가 공개 저장소로 나가지 않는다 |
| P3 | 실제 값이 박히는 산출물은 전부 `dist/` (git 제외) | 문서·인벤토리·tfvars 모두 재생성 가능 |
| P4 | 라우팅도 상징적으로 (`to: {zone: site-b}`) | 대역이 바뀌면 경로도 따라 바뀐다 |
| P5 | 배포·공개 전 `make check` 통과 필수 | 규칙 위반을 사람이 아니라 도구가 잡는다 |

**`tools/validate-site.py` 가 막는 것**

1. 랩 대역끼리 / 사무실 대역과의 충돌 — 겹치면 점프 호스트 라우팅이 깨져 **랩 접속 자체가 불가능**해진다
2. 사내 사용 중 대역(`forbidden`)과의 충돌 — **이 검사 자체가 M2 · C8 의 실무 절차**다
3. 대역 용량 부족 (구역 배분 / 랩 개수 / 공인 블록)
4. **공개 유출** — `site.local.yml` 의 값이 다른 파일에 들어갔는지 전수 검사 (`git ls-files --cached --others --exclude-standard` 기준)
5. `.gitignore` 누락

> 부수 효과로 **교육 자산이 하나 더 생겼다.** "랩 대역이 사내와 겹치면 무슨 일이 벌어지는가"를
> 말로 설명하는 대신, 교육생이 `config/site.local.yml` 을 고쳐 충돌시켜 보고
> `make check` 가 어떻게 막는지 직접 확인하게 할 수 있다.

---

### 7.8 웹 콘솔 (Hands-on Lab 방식) — 설계

> 목표: 서버에서 실행하면 웹페이지가 뜨고, 교육생이 브라우저 안에서
> **모듈 개요·목표·실습 내용·토폴로지를 보고, 버튼으로 그 모듈을 실행**한다. (Fortinet Hands-on Lab 참고)

#### 화면 구성

```
┌──────────────────────────────────────────────────────────────────────┐
│  my-network-lab            랩: [lab1 ▾]   단계: M3   상태: ● 적용됨   │
├────────────┬─────────────────────────────────┬───────────────────────┤
│ 모듈        │  M3. 라우팅 기초 — 정적 라우팅    │   토폴로지 (M3 시점)    │
│            │                                 │                       │
│ ✔ M1 L2    │  ▸ 학습 목표 (D2·D3·D4·H5)      │   pc1 ─┐              │
│ ✔ M2 IP    │  ▸ 개념                          │        ├ sw1 ─ r1 ─ r2│
│ ▶ M3 정적   │  ▸ 실습 (복붙 가능한 명령어)      │   pc2 ─┘         │    │
│   M4 OSPF  │  ▸ 관찰 포인트                   │                 r4 ─ …│
│   M6 IPv6  │  ▸ 장애 주입 챌린지               │                       │
│   …        │  ▸ 확인 문제                     │   [ 크게 보기 ]        │
│            │                                 │                       │
│            │  [ 이 모듈 적용 ] [ 연결 확인 ] [ 장애 주입 ▾ ]            │
├────────────┴─────────────────────────────────┴───────────────────────┤
│  실행 로그 (실시간 스트리밍)                                           │
│  TASK [common : netplan 설정 배포] ***  changed: [r1]                  │
└──────────────────────────────────────────────────────────────────────┘
```

#### 구조

```
브라우저 ──HTTP + SSE(로그 스트리밍)──> lab-console (FastAPI)
                                          │
                       ┌──────────────────┼──────────────────┐
                       ▼                  ▼                  ▼
              design/*.yml         modules/mXX/*.md    작업 실행기
              (토폴로지·주소)         (교재·과제)        ansible-playbook
              tools/labdesign        마크다운 렌더        terraform
                                                        scenarios/*.sh
                                                              │
                                                              ▼
                                                        Proxmox / 랩 노드
```

| 구성요소 | 선택 | 이유 |
|---|---|---|
| 백엔드 | **FastAPI (Python)** | `tools/labdesign.py` 를 그대로 재사용한다. 토폴로지·주소·모듈 상태를 이미 계산할 수 있다 |
| 프론트 | **Jinja2 + 바닐라 JS (`app.js` 1개)** | 빌드 체인·프레임워크 없음 |
| 다이어그램 | **서버에서 SVG 직접 생성** (`console/topology_svg.py`) | Mermaid.js CDN 의존 제거 → 폐쇄망에서도 그림이 뜬다 |
| 로그 스트리밍 | **SSE** | WebSocket 보다 단순하고 단방향 로그에 충분하다 |
| 작업 실행 | 서브프로세스 + **랩당 1개 큐** | 같은 랩에 두 작업이 겹치면 설정이 깨진다 |
| 되돌릴 수 없는 동작 | **경고 문구 + 영향 범위 + 최종 확인 버튼** | [이 모듈 초기화] · [랩 삭제] 는 무엇이 사라지고 무엇이 남는지 먼저 보여준다 |
| 계정 | **SQLite `var/console.db`** | pbkdf2 해시 저장. 세션 쿠키에는 username 만 담아 권한 회수가 즉시 반영된다 |
| 상태 | `var/state/lab<N>.json` | 현재 stage, 최근 작업 결과 |
| 평가 이력 | **SQLite `attempts` · `progress`** | 제출 시각·점수·틀린 문항·실패 검사. 본인/관리자 조회 (6장) |
| 실행 위치 | **Proxmox 호스트 또는 별도 서버** | 어느 Proxmox 에 붙을지는 웹에서 정한다(기본값 localhost). 상세: 7.9 |
| 실행 전 점검 | **Proxmox 헬스체크 통과 시에만 실행** | 관문은 `Runner.submit` 한 곳. 화면마다 붙이면 빠뜨린 경로가 생긴다 (7.9) |
| 인증 | **관리자 / 사용자 2종** | 사용자는 배정된 랩 하나에 대한 생성·실행·삭제 권한만. 남의 랩을 실수로 건드리는 사고를 막는다 |
| 계정 정책 | 최초 `admin/admin` → 첫 로그인 변경 강제 · 8자+특수문자 · 5회 실패 5분 잠금 | 값은 `config/site.yml` 의 `console` 절에서 조정 |

#### 기능 범위

| 단계 | 기능 |
|---|---|
| **v1 (완료)** | 모듈 목록·진도·잠금 / 교재 뷰어(교재·과제·**퀴즈·검증**·해설 탭) / 모듈별 토폴로지 SVG / [랩 생성]·[랩 삭제]·[이 모듈 적용]·[연결 확인]·[이 모듈 초기화]·[장애 주입] / **제출하고 검증 → 통과 게이팅** / **제출 이력 조회** / SSE 로그 스트리밍 / 관리자·사용자 권한 분리 + 계정 관리 / **Proxmox 연결 설정 + 상태 점검(헤더 상태 칩)** |
| v2 | 장애 주입 시나리오 실행, 교육생별 진도 기록, 검증 결과 리포트 |
| v3 | 브라우저 내 터미널(ttyd 또는 xterm.js), 패킷 캡처 뷰어, 강사용 대시보드 |

#### 이미 준비된 것

웹 콘솔이 필요로 하는 것 대부분이 Step 2~3 에서 만들어졌다.

- 모듈별 토폴로지 → `tools/render-labmap.py --stage mN` 이 이미 Mermaid 를 생성한다
- 단계별 실행 → `ansible-playbook ... -e lab_stage=mN` 이 이미 동작한다
- 검증 → `playbooks/verify.yml` 이 단계별 판정을 한다
- 초기화 → `playbooks/reset.yml`
- 랩 목록·주소 → `tools/labdesign.py`

**남은 것은 이 조각들을 감싸는 얇은 웹 레이어와, 모듈 교재(`modules/mXX/README.md`) 자체다.**

> 설계 의미: 웹 콘솔은 **CLI 를 대체하지 않고 감싼다.**
> 교육생은 버튼으로 랩을 준비하고, 학습 자체는 여전히 터미널에서 한다.
> 네트워크 엔지니어 교육에서 CLI 를 감추면 배울 것이 사라진다.

---

### 7.9 Proxmox 연결과 상태 점검 — 로컬/원격 공용

이 콘솔은 **두 가지 위치**에서 돈다. 둘을 같은 코드로 지원한다.

```
(A) Proxmox 호스트 위에서 직접        콘솔 ──> https://127.0.0.1:8006   ← 기본값
(B) 별도 서버(랩 운영 서버)            콘솔 ──> https://<원격 Proxmox>:8006   ← 권장
```

**(B) 를 권장한다.** 하이퍼바이저에 개발 도구를 얹으면 호스트가 흔들릴 때 랩 전체가 같이 죽고,
"호스트 설정을 자동으로 바꾸지 않는다"(D15)는 원칙은 실행 주체가 호스트 밖에 있을 때 지켜진다.
(B) 의 설치·연결 절차는 `docs/DEPLOY.md`, 사전 점검은 `make doctor`(`tools/preflight.py`).

접속 정보를 파일 편집으로만 바꿀 수 있으면 **콘솔만 쓰는 관리자는 손댈 방법이 없다.**
그래서 웹에서 입력받고, 그 값 하나를 Terraform · Ansible · 문서 생성기가 함께 본다.

#### 값의 흐름

```
[관리자 → 연결 설정]  주소·포트·노드·데이터스토어·토큰·인증서검증
        │
        ├──> var/console.db (settings)      원본. **토큰 비밀값은 여기에만**
        │
        └──> var/runtime.yml                파생. 토큰 제외 [git 제외]
                    │
   config/site.yml ─┴─ config/site.local.yml ─── 깊은 병합 (뒤가 이긴다)
                    ▼
              tools/labdesign.py  →  tfvars · 인벤토리 · 문서
```

| # | 규칙 | 이유 |
|---|---|---|
| X1 | 기본값은 **localhost** | 설치 직후 (A) 형태로 바로 동작한다 |
| X2 | 우선순위 `site.yml` → `site.local.yml` → `var/runtime.yml` | 파일로 관리하는 설치본도 계속 동작한다 |
| X3 | **토큰은 파일로 내보내지 않는다** | tfvars 는 생성물이라 재생성·복사된다. 실행 시 `PROXMOX_VE_API_TOKEN` 환경변수로만 넘긴다 |
| X4 | 저장하면 `labdesign.reload()` | 오래 떠 있는 프로세스가 옛 주소로 배포하는 사고를 막는다 |
| X5 | `var/runtime.yml` 도 **공개 유출 검사 대상** | 웹으로 입력한 사내 IP 도 저장소로 새면 안 된다 |

#### 최초 확인(컨펌) — 관리자 첫 로그인 직후

```
로그인 → [비밀번호 변경 강제] → [연결 설정 확인 강제] → 콘솔 사용
```

확인 전에는 관리자조차 다른 화면을 열 수 없고, 교육생은 랩을 실행할 수 없다.
`[확인하고 저장]` 은 **점검을 통과해야** 확인 처리된다.
Proxmox 를 아직 켜지 않았다면 `[점검 실패해도 확인 처리]` 로 넘어갈 수 있고, 그 사실이 DB 에 남는다.

#### 상태 점검 — 어디까지 갔는지를 남긴다

랩 작업은 **Proxmox 가 정상일 때만** 나간다. 한 번에 "안 된다"고만 하면 원인을 찾을 수 없으므로 단계로 쪼갠다.

| 단계 | 확인 | 실패 시 안내 |
|---|---|---|
| 설정 값 | 필수 항목·토큰 존재 | 무엇이 비었는지 |
| TCP 연결 | `host:8006` 소켓 | 이름 해석 / 방화벽 / 포트 구분 |
| TLS 인증서 | 검증 시도 | 자체 서명 + 검증 끔 = **정상으로 본다**\* |
| API 인증 | `/version` | 토큰 거부(401) · 권한 분리(privsep) 안내 |
| 노드 | `/nodes` | 실제 존재하는 노드 이름 목록을 보여준다 |
| 데이터스토어 | `/nodes/*/storage` | 사용 가능한 스토리지 목록 · 여유 공간 |
| 템플릿 VM | `qemu/<vmid>` | 없으면 **경고**(연결 자체는 정상) |

\* Proxmox 기본 설치가 자체 서명이다. 여기서 노란불을 켜면 **늘 켜져 있게 되고, 그러면 진짜 경고를 아무도 보지 않는다.**
노드 확인이 실패하면 그 아래 검사는 같은 이유로 실패하므로 **건너뛴다** — 원인을 하나로 보여주기 위해서다.

#### 화면 어디에서나 보이는 상태 칩

모든 화면 헤더에 `● Proxmox 정상 / 주의 / 오류` 칩을 둔다. 클릭하면 단계별 결과와 조치 힌트가 뜬다.

- 30초마다 자동 갱신. 탭으로 돌아올 때도 갱신
- 실행이 거부되면(HTTP 503) 팝업이 **자동으로 열린다** — 로그만 보고는 원인을 알 수 없다
- **교육생에게도 보여준다.** "내 설정이 틀린 건가, 랩이 죽은 건가"를 스스로 구분하는 것 자체가 I3(장애 범위 판단) 훈련이다
- 다만 조치 힌트는 관리자에게만 — 교육생이 고칠 수 있는 것이 아니다
- 점검은 30초 캐시. `deploy`/`destroy` 만 캐시를 무시하고 새로 확인한다

---

### 7.9.1 관리망은 랩 자원이 아니다 — 브리지 하나 + 랩별 VLAN

랩 운영 서버(콘솔·Terraform·Ansible)는 **모든 랩의 관리망에 발을 걸쳐야** 노드에 SSH 할 수 있다.
관리망 브리지를 랩마다 두면 두 가지가 깨진다.

| 문제 | 결과 |
|---|---|
| 랩을 만들 때마다 브리지가 새로 생긴다 | 그때마다 운영 서버에 NIC 을 붙여야 한다 |
| 랩을 지우면 브리지도 지워진다 | `terraform destroy` 가 **운영 서버의 NIC 이 꽂힌 브리지**를 지우려 든다 |

"랩을 만들 때 브리지와 NIC 을 자동으로 붙이고, 지울 때 VM → NIC → 브리지 순으로 지운다"는
방향도 검토했으나 **채택하지 않았다.**

| 막히는 지점 | 내용 |
|---|---|
| Terraform 이 자기가 도는 VM 을 관리하게 된다 | 운영 서버를 import 해야 NIC 을 추가할 수 있다. plan diff 하나로 콘솔이 도는 VM 이 재생성될 수 있고, 랩 모듈의 `ignore_changes = [network_device]` 와도 정반대다 |
| 랩 6개가 하나의 자원을 동시 수정 | `Runner` 락은 랩별이라 6개가 병렬로 돈다. 같은 VM 의 network_device 를 동시에 고치면 lost update / VM lock 오류가 난다 |
| 게스트 안쪽은 Terraform 이 못 만진다 | NIC 이 hotplug 로 붙어도 누군가 `172.30.N.9/24` 를 줘야 한다. netplan 을 미리 써두거나(=결국 사전 준비), 콘솔에 root 권한을 주거나 둘 중 하나다. 지금 콘솔은 root 권한을 쓰지 않는다 |
| hotplug 실패가 조용하다 | 게스트가 못 받으면 Proxmox 는 pending 으로 남기는데 Terraform 은 성공으로 본다 |

**게스트 쪽을 어차피 사전 준비해야 한다면 NIC 도 같이 준비하는 게 맞다.**
그래서 브리지를 **하나**만 두고 랩을 VLAN 으로 나눈다 — 운영 서버 쪽 배선이 고정된다.

```
vmbr9        VLAN-aware · 물리 포트 없음 · 전 랩 공용   ← 1회 생성 (envs/mgmt)
  ├ 랩N 노드 net0   tag = vlan_base + N                ← 랩 Terraform 이 붙인다
  └ 운영 서버 net1   태그 없음(트렁크)                   ← 1회 연결. 랩이 늘어도 그대로
vmbr{N}101 …  랩 링크 13개 + VM 13대                    ← 수시로 만들고 지운다 (envs/labN)
```

| # | 규칙 | 이유 |
|---|---|---|
| Y1 | 관리망은 `design/topology.yml` 의 `bridges` 에 없다 | 랩 자원이 아니다. `all_bridges()` 는 랩 링크만 돌려준다 |
| Y2 | 랩 노드 `net0` 만 VLAN 태그를 단다 | 태깅은 브리지가 한다 → 게스트는 untagged 만 보고 다른 VLAN 을 주입할 수 없다. 노드 설정은 랩마다 동일하게 유지된다 |
| Y3 | 운영 서버 NIC 은 **랩 수와 무관하게 1개** | 랩 생성/삭제가 VM 하드웨어를 건드리지 않는다 |
| Y4 | 배포 전 검사가 **브리지 존재 + `vlan_aware`** 를 본다 | 브리지가 없으면 VM 이 기동하지 못한다. `vlan_aware` 가 꺼져 있으면 **전 랩 관리망이 한 L2 로 합쳐지는데 통신은 되므로** 눈치채기 어렵다 — 조용히 격리만 사라진다 |
| Y5 | 연결 절차는 `dist/ops-server.md` 로 **생성**한다 | `qm set`·netplan 에 주소와 VLAN 을 손으로 적지 않는다 |
| Y6 | 브리지 comment 에 `my-network-lab` 소유 표시 | 배포 전 검사가 "남의 브리지"와 구분한다 |

> 결과: 관리망 연결은 **최초 1회**로 끝나고, 그 1회도 `qm set` 한 줄이다.
> 이후 랩을 몇 번 만들고 지우든 Proxmox 호스트도 운영 서버도 건드리지 않는다.
> D15(호스트 설정을 자동으로 바꾸지 않는다)와도 맞는다.
>
> 나중에 정말 동적으로 가고 싶다면 이 구조에서만 안전하다 — 필요한 권한이
> `ip link add/del` 하나라 좁은 sudoers 규칙으로 끊어낼 수 있다.
> NIC hotplug + netplan 재작성에는 그런 경계를 그을 수 없다.

---

### 7.9.2 교육생 SSH 키는 설정 파일이 아니라 계정에 붙는다

랩 노드 접속용 공개키를 `config/site.yml` 의 `access.ssh_public_keys` 에 모으면 두 가지가 깨진다.

| 문제 | 내용 |
|---|---|
| 범위 | 이 값은 cloud-init 으로 **전 랩 전 노드**에 박힌다. 교육생 A 가 B 의 랩 노드에 로그인할 수 있다 — 운영 서버를 ProxyJump 로 쓰면 모든 관리망에 닿고, 점프 계정도 공유다. 캡스톤 시험 중에 남의 랩을 고쳐 줄 수 있다 |
| 반영 | cloud-init 의 사용자/SSH 모듈은 인스턴스당 한 번 돈다. 명단이 바뀌면 `terraform apply` + **노드 13대 재부팅**이 필요하다 |

콘솔에는 이미 사용자별 랩 배정이 있는 계정 체계가 있다. 키를 거기에 붙인다.

```
교육생이 콘솔 [접속 키] 에 자기 공개키 입력
      ↓  console/sshkeys.py 가 검증 (개인키·옵션 주입·여러 줄 거절)
var/console.db  users.ssh_key
      ↓  tools/gen-inventory.py — **배정된 랩의** group_vars 에만
roles/common → authorized_keys (exclusive)
```

| # | 규칙 | 이유 |
|---|---|---|
| K1 | `site.yml` 에는 **운영 서버 키만** | Ansible 이 처음 로그인할 부트스트랩 키다. cloud-init 말고는 넣을 방법이 없다 |
| K2 | 교육생 키는 `var/console.db`, 배포는 Ansible | 배정된 랩에만 들어가고, 재부팅 없이 다음 설정 적용에 반영된다 |
| K3 | 비활성 계정의 키는 배포 목록에서 빠진다 | 퇴소 처리가 계정 비활성화 한 번으로 끝난다 |
| K4 | `authorized_keys` 는 `exclusive` | 노드에 손으로 넣은 키도 초기화 때 사라진다 — "랩 초기화"의 의미를 접속 경로까지 넓힌다 |
| K5 | **관리자 키가 비면 설정 적용을 중단한다** | K4 와 합쳐지면 운영 서버 자신의 키까지 지워 **모든 노드에서 영구히 잠긴다.** SSH 로만 관리하므로 복구 수단이 없다 |
| K6 | 입력값은 파싱해서 검증한다 | 개인키 오붙여넣기(가장 흔한 사고), `command=` 같은 authorized_keys 옵션 주입, 줄바꿈으로 여러 키 밀어넣기를 막는다 |
| K7 | 노드에 `/etc/lab-authorized` 를 남긴다 | 누가 이 노드에 들어올 수 있는지 노드 안에서 확인할 수 있다 |

> 남은 것: 점프 계정(`trainee`)이 아직 공유다. 교육생별 계정으로 나누고 자기 랩 대역으로만
> 나가게 제한하는 것은 별도 과제다 (`dist/access.md` 8.2 가 요구하지만 미구현).

---

### 7.10 같은 Proxmox 의 다른 자원을 건드리지 않는다 (블라스트 반경)

이 랩은 **회사 Proxmox 위에서 돈다.** 같은 호스트에 운영 VM 이 함께 있다는 뜻이다.
"우리 것만 건드린다"를 주석이 아니라 **동작으로** 보장한다.

**설계상 이미 격리된 것**

| 자원 | 격리 방식 |
|---|---|
| 랩 링크 | 브리지에 **물리 NIC 을 붙이지 않는다** (`ports` 미지정). L2 가 호스트 밖으로 못 나간다 |
| 브리지 이름 | `vmbr{lab_id}{bridge_id}` — 랩마다 다르고 운영 브리지와도 다르다 |
| 관리망 | **랩 자원이 아니다.** 전 랩 공용 브리지 1개 + 랩별 VLAN (`envs/mgmt` 가 1회 생성) — 7.9.1 |
| VM 이름·태그 | `lab{N}-{node}` + `my-network-lab` 태그. 목록에서 즉시 구분된다 |
| 라우팅 | 랩 노드만 포워딩. 사내 라우터에 랩 대역 경로를 넣지 않는다 |
| Ansible 대상 | 인벤토리에 있는 노드만. 시나리오도 인벤토리 안에서만 동작한다 |

**찾아서 고친 것**

| # | 문제 | 무슨 일이 벌어질 수 있었나 | 조치 |
|---|---|---|---|
| S1 | 인벤토리에 `ansible_host` 가 없었다 | 노드 이름이 `web` · `dns` · `ftp` · `edge` 다. 컨트롤러의 DNS 검색 도메인이 이 이름을 **사내 실서버**로 풀면, 랩 설정 플레이북이 그 서버에서 실행된다 (hostname 변경 · netplan 재작성 · nftables 설치) | 관리망 IP 를 인벤토리에 **못 박았다**. 이름 해석이 개입할 여지를 없앴다 |
| S2 | VMID 가 `101~913` | Proxmox 는 새 VM 에 100 번부터 준다. 운영 VM 과 번호가 섞여 **목록에서 헷갈려 잘못 지우는** 사고가 난다 | `900000 + lab_id×100 + 노드번호` 로 이동 (lab1 = 900101~) |
| S3 | 템플릿 VMID 를 확인하지 않았다 | 그 번호에 운영 VM 이 있으면 **운영 VM 을 복제**한다 | 배포 전 `template=1` 인지 확인. 아니면 거부 |
| S4 | 브리지 이름 충돌을 확인하지 않았다 | 같은 이름의 기존 브리지(물리 포트 포함)를 랩 것으로 여기고 **삭제**할 수 있다 | 배포 전 확인. 이미 있으면 거부 |
| S5 | 랩 대역과 호스트 대역의 실제 충돌 | 설정 파일끼리만 비교했다. **실제 호스트가 무엇을 쓰는지는 안 봤다** | 호스트 인터페이스 목록을 조회해 겹치면 거부(관리망) · 경고(그 외) |
| S6 | 관리망에서 하이퍼바이저에 접근 가능 | 교육생이 랩 VM 에서 Proxmox 호스트를 건드릴 수 있다 | `dist/host-guard.nft` 생성 (적용은 관리자 판단) |

**배포 전 검사** — `[랩 생성]` 을 누르면 Terraform 보다 먼저 Proxmox 에 물어본다.
하나라도 걸리면 **아무것도 만들지 않고** 무엇과 부딪히는지 이름을 대고 멈춘다.

```
VMID 900101~900113  →  이 구간을 다른 VM 이 쓰고 있다: 900105(billing-db), 900107(erp-app)
템플릿 VM 9000      →  VMID 9000 는 템플릿이 아니다 (이름: erp-report-svr)
브리지 이름          →  같은 이름의 브리지가 이미 있다: vmbr1101(포트: eno2)
호스트 네트워크 대역   →  관리망 172.30.0.0/16 이 호스트의 vmbr9(172.30.0.1/16) 과 겹친다
```

**하지 않기로 한 것**

- **Proxmox 방화벽은 켜지 않는다.** 데이터센터 전역 스위치를 켜야 동작하므로 같은 호스트의
  다른 VM 정책까지 바뀐다. 대신 policy 가 `accept` 이고 drop 만 더하는 별도 nftables 테이블을 만들어 둔다
- **호스트 설정을 자동으로 바꾸지 않는다.** 파일만 생성하고, 적용 여부와 시점은 관리자가 정한다

> 남은 위험은 문서에 적어 둔다 (`dist/access.md` 6.1). 브리지를 만들 때 Proxmox 가
> 호스트 네트워크를 다시 적용하므로, **호스트에 미적용 변경이 남아 있으면** 그것까지 함께 반영된다.
> 배포 전 검사가 이 상태를 경고한다.

---

## 8. 결정 사항 (확정)

| # | 항목 | **확정** | 비고 |
|---|---|---|---|
| D1 | 플랫폼 | **Proxmox VE 단독** | 토폴로지 시각화 보완책 7.3.2 적용 |
| D2 | 노드 이미지 | **오픈소스 통일 (FRR + Linux Bridge + Ubuntu)** | 벤더 CLI 매핑표는 부록으로 |
| D3 | AWS | **설계만, 구현 보류** | R1~R5 규칙 준수로 이식성 확보 |
| D4 | 자동화 | **Terraform + Ansible 완전 코드화** | Proxmox provider는 `bpg/proxmox` 사용 |

| D5 | 랩 접근 경로 | **사내망과 라우팅 미연결. Proxmox 웹 콘솔 + 점프 호스트 SSH 2경로만 허용** | 상세: `dist/access.md` (생성물) |
| D6 | 랩 주소 대역 | **`config/site.yml` 이 단독 소유.** 기본값은 공개 안전 대역, 실제 값은 `config/site.local.yml` | 충돌 검사: `tools/validate-site.py` |

| D7 | 운영 형태 | **1인 1랩 (linked clone)** | 관리망만 랩별 분리, 서비스망은 전 랩 동일 |
| D8 | 호스트 사양 | **RAM 64GB 이상** → 동시 6랩 권장 | 랩당 13노드 / 8.96GB |
| D10 | 공개 배포 | **`config/site.yml` / `site.local.yml` 분리 + `validate-site.py` 게이트** | 상세: 7.7 |
| D11 | 웹 콘솔 순서 | **M0~M1 로 교재 포맷 확정 → 콘솔 v1 → 나머지 모듈** | 콘솔이 렌더할 실물이 있는 상태로 설계 |
| D12 | 웹 콘솔 사용자 | **교육생 개인별 접속** (자기 랩만 조회·실행) | 1인 1랩 구조와 정합, 진도 기록으로 확장 |
| D13 | Proxmox 연결 | **웹에서 입력 · 기본값 localhost · 관리자 최초 확인 강제 · 점검 통과 시에만 실행** | 로컬/원격 동일 코드. 상세: 7.9 |
| D14 | 서술 과제 검토 | **비차단 리뷰 + 캡스톤만 승인 필수.** 루브릭 충족 시 자동 통과 | 관리자를 병목에 두지 않는다. 상세: 6.7 |
| D15 | 블라스트 반경 | **VMID 900000 대역 이동 + 배포 전 충돌 검사 + 호스트 보호 규칙(수동 적용)** | Proxmox 전역 방화벽은 켜지 않는다. 상세: 7.10 |

**추후 확인 필요**
- D9. 회사 실장비 벤더 → **부록 ④에 빈 열로 남겨 두었다.** 장비가 정해지면 `docs/appendix/vendor-cli.md.j2` 의 `회사 실장비` 열을 채워 사내 문서로 쓴다

---

## 9. 산출물 디렉토리 구조 (확정)

```
my-network-lab/
├── README.md
├── PLAN.md
├── design/
│   ├── topology.yml           # 노드/링크/브리지 정의 (Single Source of Truth)
│   ├── ipam.yml               # IPv4 / IPv6 / VLAN 설계
│   └── lab-map.md             # 교육생용 랩 지도 1페이지 (자동 생성)
├── infra/
│   ├── terraform/
│   │   ├── modules/proxmox/   # Proxmox VM + 브리지 프로비저닝
│   │   ├── modules/aws/       # (보류) 동일 입력 스키마 유지
│   │   └── envs/lab01/
│   └── ansible/
│       ├── inventory/         # topology.yml에서 생성
│       ├── roles/{router,switch,host,server,edge}/
│       └── playbooks/{site.yml,module-XX.yml,reset.yml}
├── modules/
│   ├── m00-orientation/ ... m11-troubleshooting/
│   │   ├── README.md          # 개념 + 실습 지시서 (Mermaid 토폴로지 포함)
│   │   ├── tasks.md           # 실습 과제
│   │   ├── answers.md         # 정답·해설 (교육 담당자용)
│   │   └── verify.sh          # 자동 검증
├── console/                   # 웹 콘솔 (FastAPI). topology_svg.py 로 다이어그램 자체 생성
├── scenarios/                 # 장애 주입 (mXX-NN.yml, break/fix 겸용)
├── captures/                  # 분석용 샘플 pcap
├── tools/
│   ├── render-diagram.py      # topology.yml → Mermaid
│   ├── gen-inventory.py       # topology.yml → Ansible inventory
│   └── lab-whereami           # 노드 내부 헬퍼
└── docs/
    ├── cheatsheet.md
    ├── troubleshooting.md
    ├── glossary.md
    └── vendor-cli-mapping.md  # FRR ↔ Cisco IOS ↔ 실장비
```

---

## 10. 진행 로드맵

| Step | 산출물 | 상태 |
|---|---|---|
| 0 | 마스터 플랜 (이 문서) | ✅ 완료 |
| 1 | 도구·플랫폼 결정 (D1~D4) | ✅ 완료 |
| 2 | **전체 토폴로지 + IPv4/IPv6/VLAN 설계서** (`design/topology.yml`, `design/ipam.yml`, `dist/lab-map.md` ← `tools/render-labmap.py`) | ✅ 완료 |
| 3 | Terraform(Proxmox) + Ansible 골격, 골든 템플릿, 랩 기동·초기화 절차 | ✅ 완료 |
| 3.5 | **공개 배포 구조** — `config/site.yml` 분리, 주소 계산 엔진, `validate-site.py` | ✅ 완료 |
| 4 | **M0 오리엔테이션 + M1 (L2/스위칭)** — 교재 포맷 확정, 장애 주입 체계 | ✅ 완료 |
| 5 | **웹 콘솔 v1** — 모듈 뷰어 · 토폴로지 · 실행 버튼 · 로그 스트리밍 · 교육생 인증 (7.8) | ✅ 완료 |
| 5.5 | **모듈 평가 체계** — 퀴즈·검사·게이팅·이력, [이 모듈 초기화] 확인 절차 (6장) | ✅ 완료 |
| 5.6 | **Proxmox 연결 설정 + 상태 점검** — 로컬/원격 공용, 최초 확인 강제, 실행 게이트, 헤더 상태 칩 (7.9) | ✅ 완료 |
| 5.7 | **서술 과제 + 검토 큐** — 루브릭 자동 선별, 비차단 진행, 관리자 승인(캡스톤) (6.7) | ✅ 완료 |
| 5.8 | **블라스트 반경 차단** — `ansible_host` 고정, VMID 재배치, 배포 전 충돌 검사, 호스트 보호 규칙 (7.10) | ✅ 완료 |
| 5.9 | **시험 세션(제한 시간 + 점수 동결)** — 마감 시각 확정본, 실행 잠금, 무작위 장애 주입 (6.8) | ✅ 완료 |
| 6 | M2 → M11 순차 제작 (교재 + 퀴즈 + 검사 + 서술 세트) | ✅ 완료 — **M0~M11 전 모듈** (퀴즈 100문항 · 검사 85개 정의 / 캡스톤은 그중 64개를 한 번에 · 서술 13개 · 장애 시나리오 32개) |
| 7 | 부록 (치트시트 / 트러블슈팅 플로우차트 / 용어집 / 벤더 CLI 매핑) | ✅ 완료 — `docs/appendix/*.md.j2` → `dist/appendix/` · 웹 콘솔 [부록] 화면 · 인쇄 스타일 |
| 7.5 | **배포 자동화** — `install.sh`(패키지·Terraform·venv·SSH 키·systemd) · `make doctor` 사전 점검 · `make pack` 이관 · `docs/DEPLOY.md` | ✅ 완료 |
| 7.7 | **교육생 SSH 키 자율 등록** — 콘솔 [접속 키] · 랩별 배포 · 잠금 사고 방지 가드 (7.9.2) | ✅ 완료 |
| 7.6 | **관리망 VLAN 트렁크화** — 전 랩 공용 브리지 1개 + 랩별 VLAN · 운영 서버 NIC 1개 고정 · 배포 전 `vlan_aware` 검사 (7.9.1) | ✅ 완료 |
| 8 | (선택) 웹 콘솔 v2/v3 — 장애 주입 UI · 진도 기록 · 웹 터미널 | ⬜ |
| 9 | (선택) AWS 이식 실제 구현 + M11 확장 모듈 | ⬜ 보류 |

> **Step 4 를 M0+M1 두 개로 잡은 이유**: 모듈 문서 포맷(개요·목표·실습·검증·장애주입)이 확정되어야
> 콘솔이 렌더할 대상을 알 수 있고, 반대로 콘솔이 있어야 교재를 어떤 단위로 쪼갤지 결정된다.
> 두 개를 먼저 쓰고 콘솔을 붙이면, 나머지 8개는 확정된 틀에 채워 넣기만 하면 된다.

**원칙: 한 번에 전부 만들지 않는다. 모듈 단위로 완성 → 검토 → 다음.**

---

> 미결 항목·나중 할 일은 [`docs/TODO.md`](docs/TODO.md) 에 따로 모아 둔다.
