<!-- 자동 생성. 원본: modules/m07-packet-analysis/README.md.j2  ·  생성: make modules -->

# M7. 패킷 분석 — tcpdump 심화

> **소요 시간** {{ meta.duration }} · **랩 단계** `{{ stage }}` · **역량** {{ meta.competencies | join(', ') }}

M1 부터 지금까지 캡처를 여러 번 떴다. 그런데 **"어디서 뜰 것인가"는 늘 교재가 정해 줬다.**

이번 모듈에서는 그것을 **스스로** 정한다. 그리고 그것이 tcpdump 실력의 본질이다 —
필터 문법이 아니라 **캡처 지점 선정**.

> **필터는 검색하면 나온다. 지점 선정은 안 나온다.**
> 망 구조를 알아야 하고, 무엇을 증명하려는지 알아야 정할 수 있기 때문이다.

## 학습 목표

{% for o in meta.objectives %}
{{ loop.index }}. {{ o }}
{% endfor %}

## 사전 지식 체크

- pc1 → web 패킷이 지나는 노드를 **순서대로** 말할 수 있는가? (M3)
- 트렁크 링크에서 `icmp` 필터가 안 먹는 이유는? (M2)
- "연결은 되는데 응답이 없다" 를 캡처에서 어떻게 확인하는가? (M6)

---

## 1. 이 단계의 랩

```mermaid
{{ topology }}
```

**새 노드는 없다.** 지금까지 만든 전 구간이 캡처 대상이다.

| 지점 | 인터페이스 | 무엇이 보이나 |
|---|---|---|
| `pc1` | `eth1` | **출발지에서 나가는 그대로** (태그 없음) |
| `sw1` | `eth1`/`eth2` | access 포트 — 태그 없음 |
| `sw1` | `eth3` | **트렁크 — 802.1Q 태그가 붙어 있다** |
| `r1` | `eth1` | 트렁크 (태그 있음) |
| `r1` | `eth1.10` | 태그가 벗겨진 뒤 |
| `r1`~`r4` | `eth2`/`eth3` | P2P 구간 — **MAC 이 홉마다 바뀐 것** |
| `web` | `eth1` | **목적지에 도착한 그대로** |

> **같은 패킷이 지점마다 다르게 보인다.** MAC 은 홉마다 바뀌고(M2), VLAN 태그는 붙었다 벗겨진다.
> **"어느 지점에서 뜬 캡처인가"를 모르면 해석이 통째로 틀어진다.**

---

## 2. 개념

### 2.1 ★★ 캡처 지점 선정이 실력이다 (H1)

**질문은 언제나 하나다 — "나는 무엇을 증명하려 하는가?"**

| 증명하려는 것 | 어디서 뜨나 |
|---|---|
| 패킷이 **나가기는 하는가** | **출발지** |
| 패킷이 **도착하는가** | **목적지** |
| **어디서 사라지는가** | 출발지·목적지 사이를 **이등분** |
| **변형되는가** (NAT · VLAN · MTU) | 그 장비의 **양쪽 인터페이스** |
| **응답이 돌아오는가** | 목적지 (요청과 응답을 함께 본다) |

**기본 순서**

```
① 출발지    — 나가는가?          (안 나가면 경로·소켓 문제. 다른 곳 볼 필요 없다)
② 목적지    — 도착하는가?        (도착하면 문제는 반대 방향이나 응답 쪽)
③ 그 사이   — 이등분해서 좁힌다   (홉 8개면 3번이면 끝난다)
```

> **①을 건너뛰는 것이 가장 흔한 실수다.**
> 목적지부터 보고 "아무것도 안 오네요" 라고 말하는 것은 아무 정보도 아니다.
> 안 나갔을 수도, 중간에 사라졌을 수도 있다.

**이등분이 왜 이기는가**

```
홉이 8개일 때
  순서대로  : 최대 8번 캡처
  이등분    : 8 → 4 → 2 → 1,  최대 3번
```

> 실무에서 캡처 한 번은 **"그 장비에 접속해서 · 권한을 받고 · 재현을 기다리는" 시간**이다.
> 3번과 8번의 차이는 하루와 이틀의 차이다.

**실장비에서는 어떻게 뜨는가**

| 방법 | 설명 | 주의 |
|---|---|---|
| 장비에서 직접 | 리눅스·일부 라우터에서 `tcpdump` | **이 랩의 방식** |
| **포트 미러링 (SPAN)** | 스위치가 특정 포트 트래픽을 복사해 보내 준다 | 스위치 CPU 부하 · 과부하 시 **누락** |
| **TAP** | 물리적으로 신호를 분기하는 장비 | 가장 정확. 장비 값이 든다 |

> **미러링은 "복사본" 이라 누락될 수 있다.** "캡처에 안 보인다" 가 곧 "안 지나갔다" 는 아니다.
> 정확성이 중요한 조사에서는 TAP 을 쓴다. 이 사실을 모르면 잘못된 결론을 내린다.

### 2.2 BPF 필터 (H2)

**기본 요소**

| 종류 | 예 |
|---|---|
| 호스트 | `host {{ ip.web }}` · `src host {{ ip.pc1 }}` · `dst host {{ ip.web }}` |
| 대역 | `net {{ cidr.vlan40 }}` · `net {{ zone['site-b'] }}` |
| 포트 | `port 80` · `src port 443` · `portrange 50000-50100` |
| 프로토콜 | `tcp` · `udp` · `icmp` · `arp` · `icmp6` |
| 링크 | `ether host {{ mac.pc1 }}` · `ether broadcast` · **`vlan`** |
| 방향 | `-Q in` / `-Q out` (옵션) |

**조합**

```bash
tcpdump -i eth1 -nn 'host {{ ip.web }} and tcp port 80 and not port 22'
tcpdump -i eth1 -nn 'icmp or arp'
tcpdump -i eth1 -nn 'net {{ cidr.vlan40 }} and not net {{ cidr.vlan10 }}'
```

**비트 필터 — 플래그를 직접 본다**

```bash
# SYN 또는 SYN-ACK (SYN 비트가 켜진 것 전부)
tcpdump -i eth1 -nn 'tcp[tcpflags] & tcp-syn != 0'

# SYN 만 (연결 시도만. SYN-ACK 제외)
tcpdump -i eth1 -nn 'tcp[tcpflags] & (tcp-syn|tcp-ack) == tcp-syn'

# RST — 거부당한 연결만
tcpdump -i eth1 -nn 'tcp[tcpflags] & tcp-rst != 0'

# ICMP echo request 만
tcpdump -i eth1 -nn 'icmp[icmptype] == 8'
```

> **`!= 0` 과 `== tcp-syn` 의 차이가 핵심이다.**
> 앞은 "그 비트가 켜져 있다", 뒤는 "그 비트만 켜져 있다".
> "누가 연결을 시도했는가" 를 셀 때 앞의 것을 쓰면 **응답까지 두 배로 세게 된다.**

**꼭 붙일 것 두 개**

| 옵션 | 왜 |
|---|---|
| **`-nn`** | 이름·포트 해석을 안 한다. 안 붙이면 **tcpdump 가 DNS 조회를 하고**, 그 트래픽이 또 잡힌다 |
| **`not port 22`** | 내 SSH 세션이 통째로 섞인다. 출력이 SSH 로 나가며 **되먹임**까지 생긴다 |

> **되먹임(feedback loop)을 한 번 겪어 볼 것.** SSH 로 접속한 노드에서
> `tcpdump -i <관리 인터페이스>` 를 필터 없이 걸면 화면이 폭주하며 멈추지 않는다.
> 원인은 "트래픽이 많아서" 가 아니라 **내가 만든 출력이 다시 잡히기 때문**이다.

**트렁크에서는 `vlan` 을 앞에 붙인다** (M2 에서 겪은 함정)

```bash
tcpdump -i eth1 -e -nn 'vlan and icmp'      # 트렁크
tcpdump -i eth1 -e -nn 'icmp'               # access 포트
```

### 2.3 옵션 — 무엇을 얼마나 볼 것인가

| 옵션 | 뜻 | 언제 |
|---|---|---|
| `-e` | 링크 계층 헤더까지 | **MAC 을 봐야 할 때** (M2 의 그 실습) |
| `-nn` | 이름·포트 해석 안 함 | **항상** |
| `-v` / `-vv` | 상세도 (TTL · 체크섬 · 옵션) | TTL 이나 IP 옵션을 볼 때 |
| `-X` / `-XX` | 16진수 + ASCII 덤프 | **페이로드를 봐야 할 때** |
| `-c N` | N개만 잡고 종료 | 빠른 확인 |
| `-s N` | snaplen — 패킷당 저장 길이 | 고속 링크 · **민감 정보 회피** |
| `-w file` | 파일로 저장 | **나중에 분석 / 남에게 전달** |
| `-r file` | 파일 읽기 | 분석 · **필터를 다시 걸 수 있다** ★ |
| `-C N -W M` | 링 버퍼 (N MB × M개) | **간헐적 장애를 기다릴 때** |
| `-i any` | 모든 인터페이스 | 어디로 나가는지 모를 때 |

> **`-w` 로 뜨고 `-r` 로 거르는 것이 정석이다.**
> 화면으로 보면 한 번 지나간 것은 끝이다. 파일로 뜨면 **필터를 몇 번이고 다시 걸 수 있다.**
> 현장에서는 "일단 넓게 떠서 저장하고, 분석은 자리에 돌아와서" 가 기본이다.

### 2.4 다중 지점 동시 캡처 (H3)

**패킷이 사라지는 구간을 특정하는 유일한 방법이다.**

```
pc1 ──── r1 ──── r2 ──── r4 ──── web
 ①              ②              ③
```

세 곳에서 **동시에** 뜨고, 같은 패킷을 찾아 대조한다.

| ① | ② | ③ | 결론 |
|---|---|---|---|
| 있음 | 있음 | 있음 | 이 방향은 정상. **반대 방향**을 본다 |
| 있음 | 있음 | 없음 | **r2 ~ web 구간**에서 사라진다 |
| 있음 | 없음 | 없음 | **pc1 ~ r2 구간**에서 사라진다 |
| 없음 | — | — | 애초에 안 나갔다. **출발지 문제** |

**같은 패킷을 찾는 방법**

| 단서 | 설명 |
|---|---|
| **IP ID** | IPv4 헤더의 식별자. `-v` 로 보인다. 홉을 넘어도 그대로 |
| TCP seq | 재전송 추적에 유용 |
| ICMP id/seq | ping 추적에 최적 |
| 시각 | 장비 시계가 맞아야 한다 (**NTP 가 안 맞으면 대조가 지옥이 된다**) |

> **홉을 넘으면 MAC 은 바뀐다.** MAC 으로 같은 패킷을 찾으려 하면 안 된다.
> IP·포트·seq 처럼 **엔드투엔드로 유지되는 값**을 기준으로 잡는다 (M2 의 그 원칙).

### 2.5 캡슐화를 바이트로 짚는다 (A2)

```bash
tcpdump -i eth1 -nn -X -c 1 'tcp port 80'
```

```
0x0000:  5254 0031 0001 5254 0011 0001 0800 4500
         └──── 목적지 MAC ────┘└─ 출발지 MAC ──┘ └타입┘└IP
0x0010:  003c 1c46 4000 4006 ...
```

| 바이트 | 무엇 |
|---|---|
| `0 ~ 13` | **Ethernet 헤더** (목적지 MAC 6 + 출발지 MAC 6 + 타입 2) |
| `14 ~ 33` | **IP 헤더** (보통 20바이트. 첫 바이트 `45` = 버전4/길이5) |
| `34 ~ 53` | **TCP 헤더** (보통 20바이트) |
| `54 ~` | **페이로드** (HTTP 요청 등) |

> **`0800` 을 찾으면 그 뒤가 IP 다.** `45` 로 시작하면 IPv4 이고 헤더 길이 20바이트.
> 태그가 있으면 `8100` 이 먼저 나오고 4바이트 뒤에 `0800` 이 온다 — **그래서 오프셋이 밀린다**(2.2).
>
> 이 계산이 되면 "왜 필터가 안 먹지" 를 스스로 설명할 수 있다.

### 2.6 pcap 을 다루는 법 (H4)

```bash
# 저장
sudo tcpdump -i eth1 -w /tmp/x.pcap 'host {{ ip.web }} and not port 22'

# 읽기 — 저장할 때와 다른 필터를 걸 수 있다 ★
tcpdump -r /tmp/x.pcap -nn 'tcp[tcpflags] & tcp-rst != 0'

# 통계
tcpdump -r /tmp/x.pcap -nn | wc -l
```

**Wireshark 로 넘길 때**

```bash
# 점프 호스트로 회수
scp pc1:/tmp/x.pcap .
```

| Wireshark 기능 | 쓸 곳 |
|---|---|
| **Follow → TCP Stream** | HTTP 요청·응답을 통째로 읽는다 |
| **Statistics → Conversations** | 누가 누구와 얼마나 주고받았나 |
| **Statistics → IO Graph** | 언제 끊겼나 (시간축) |
| **Expert Information** | 재전송 · 중복 ACK 자동 표시 |
| 표시 필터 `tcp.analysis.retransmission` | 손실 구간 찾기 |

> **BPF 필터(캡처용)와 Wireshark 표시 필터는 문법이 다르다.**
> `port 80` (BPF) ↔ `tcp.port == 80` (Wireshark). 섞어 쓰면 안 먹는다.

> **pcap 은 민감 정보 덩어리다.** 평문 프로토콜이면 아이디·비밀번호가 그대로 들어 있다 (M8 에서 확인한다).
> 밖으로 내보낼 때는 **snaplen 으로 헤더만 뜨거나**, 반출 승인을 받는다.
> 이 감각이 없으면 "장애 분석하려고 캡처를 메신저로 보냈다" 는 사고가 난다.

---

## 3. 실습

> 터미널을 **많이** 연다. 이 모듈은 동시에 여러 곳을 봐야 한다.
> 모든 캡처에 **`-nn`** 과 **`not port 22`** 를 붙이는 습관을 들일 것.

### 실습 1 — 되먹임을 겪어 본다

```bash
# pc1 — 관리 인터페이스에 필터 없이
sudo tcpdump -i {{ mgmt_if }}
```

**화면이 멈추지 않는다.** Ctrl-C 로 끊고 다시 해 본다.

```bash
sudo tcpdump -i {{ mgmt_if }} -nn 'not port 22'
```

조용해진다.

> **원인은 트래픽이 많아서가 아니다.** tcpdump 출력이 SSH 로 나가고,
> 그 SSH 패킷을 tcpdump 가 또 잡고, 그 출력이 또 나간다. **자기 자신을 먹는 되먹임**이다.
> `not port 22` 하나로 끝난다. 이 한 줄을 몸에 붙이는 것이 이 실습의 목적이다.

### 실습 2 — 필터를 좁혀 간다 (H2)

**터미널 web** — 트래픽을 만든다.

```bash
# pc1 에서 반복
while true; do curl -s -o /dev/null http://{{ ip.web }}/; sleep 1; done
```

**터미널 pc1** — 필터를 하나씩 좁혀 가며 줄 수를 비교한다.

```bash
sudo tcpdump -i eth1 -nn -c 20                                  # 전부
sudo tcpdump -i eth1 -nn -c 20 'host {{ ip.web }}'              # 상대만
sudo tcpdump -i eth1 -nn -c 20 'host {{ ip.web }} and port 80'  # 그 포트만
sudo tcpdump -i eth1 -nn -c 20 'tcp[tcpflags] & (tcp-syn|tcp-ack) == tcp-syn'   # 연결 시도만
```

마지막 것은 **1초에 한 줄**만 나온다. curl 한 번에 연결 하나이기 때문이다.

```bash
# 대조 — SYN 비트가 켜진 것 전부
sudo tcpdump -i eth1 -nn -c 20 'tcp[tcpflags] & tcp-syn != 0'
```

**두 배로 나온다.** SYN-ACK 도 SYN 비트가 켜져 있기 때문이다.

> **"연결이 몇 개 들어왔나" 를 세는데 이 필터를 쓰면 항상 두 배로 센다.**
> 실무에서 흔한 착오다.

### 실습 3 — 지점마다 다르게 보인다 (H1)

**같은 ping 을 세 지점에서 동시에 뜬다.**

```bash
# 터미널 A — pc1 (출발지, access 포트)
sudo tcpdump -i eth1 -e -nn icmp

# 터미널 B — r1 (트렁크)
sudo tcpdump -i eth1 -e -nn 'vlan and icmp'

# 터미널 C — r1 (P2P 구간, 태그 없음)
sudo tcpdump -i eth2 -e -nn icmp

# 터미널 D — web (목적지)
sudo tcpdump -i eth1 -e -nn icmp
```

```bash
# 터미널 E — pc1
ping -c 3 {{ ip.web }}
```

**네 화면을 나란히 놓고 비교한다.**

| 지점 | 출발지 MAC | 목적지 MAC | VLAN 태그 | 출발지 IP | 목적지 IP |
|---|---|---|---|---|---|
| A (pc1 eth1) | `{{ mac.pc1 }}` | `{{ mac.r1 }}` | 없음 | `{{ ip.pc1 }}` | `{{ ip.web }}` |
| B (r1 eth1) | `{{ mac.pc1 }}` | `{{ mac.r1 }}` | **{{ vlans[10].id }}** | `{{ ip.pc1 }}` | `{{ ip.web }}` |
| C (r1 eth2) | `{{ ifs.r1['eth2'].mac }}` | `{{ ifs.r2['eth1'].mac }}` | 없음 | `{{ ip.pc1 }}` | `{{ ip.web }}` |
| D (web eth1) | `{{ ifs.r4['eth3'].mac }}` | `{{ mac.web }}` | 없음 | `{{ ip.pc1 }}` | `{{ ip.web }}` |

> **IP 두 열은 처음부터 끝까지 같다. MAC 두 열은 매번 다르다.**
> M2 에서 배운 것이 전 구간에서 성립한다는 것을 여기서 확인한다.
>
> 그리고 **B 에서 `vlan` 을 빼면 아무것도 안 잡힌다.** 직접 해 볼 것.

### 실습 4 — ★ 사라지는 구간을 찾는다 (H3)

**담당자가 `m07-02` 를 주입한 상태에서 진행한다.** (아직 주입하지 않았다면 5장 먼저)

증상: **pc1 에서만** web 의 HTTP 가 안 된다. pc2 는 정상이고 ping 도 정상이다.

**① 출발지부터**

```bash
# pc1
sudo tcpdump -i eth1 -nn 'host {{ ip.web }} and tcp port 80'
# 다른 창에서
curl -m 5 http://{{ ip.web }}/
```

SYN 이 **나간다.** → 출발지 문제는 아니다.

**② 목적지**

```bash
# web
sudo tcpdump -i eth1 -nn 'host {{ ip.pc1 }} and tcp port 80'
```

**아무것도 안 온다.** → 중간에서 사라진다.

**③ 이등분 — 가운데(r2)**

```bash
# r2
sudo tcpdump -i eth1 -nn 'host {{ ip.pc1 }} and tcp port 80'      # r1 쪽
sudo tcpdump -i eth2 -nn 'host {{ ip.pc1 }} and tcp port 80'      # r4 쪽
```

**들어오고 나간다.** → 문제는 r2 **뒤쪽** 절반이다.

**④ r4**

```bash
# r4
sudo tcpdump -i eth1 -nn 'host {{ ip.pc1 }} and tcp port 80'      # r2 쪽 (들어오는 곳)
sudo tcpdump -i eth3 -nn 'host {{ ip.pc1 }} and tcp port 80'      # 서버망 쪽 (나가는 곳)
```

**들어오는데 나가지 않는다.** → **r4 에서 버려지고 있다.**

```bash
# r4 — 확인
sudo nft list ruleset
```

> **네 번의 캡처로 홉 4개 중 한 곳을 특정했다.**
> 순서대로 뜨면 더 걸렸을 것이고, 목적지부터 봤다면 "안 오네요" 로 끝났을 것이다.
>
> **"들어오는 인터페이스에는 있는데 나가는 인터페이스에는 없다"** —
> 이것이 **그 장비가 버렸다**는 결정적 증거다. 장비 하나에서도 양쪽을 봐야 하는 이유다.

### 실습 5 — 재전송을 읽는다

```bash
# 담당자가 m07-01 을 주입한 상태에서
# pc1
sudo tcpdump -i eth1 -nn -v 'host {{ ip.web }} and tcp port 80'
```

```bash
# 다른 창
curl -s -o /dev/null -w 'time=%{time_total}\n' http://{{ ip.web }}/
```

**같은 `seq` 가 여러 번 나가는 것**이 보인다.

```
{{ ip.pc1 }}.41300 > {{ ip.web }}.80: Flags [P.], seq 1:80, length 79
{{ ip.pc1 }}.41300 > {{ ip.web }}.80: Flags [P.], seq 1:80, length 79   ← 같은 seq. 재전송이다
```

> **재전송 = 어딘가에서 버려졌다.** "가끔 느립니다" 의 정체가 이것이다.
> 통신은 되므로 ping 으로는 안 잡히고, **캡처에서만 보인다.**
>
> 다음 할 일은 정해져 있다 — **구간을 나눠 어디서 사라지는지 찾는다** (실습 4 의 방법).

Wireshark 로 열면 더 분명하다.

```bash
scp pc1:/tmp/x.pcap .      # 점프 호스트에서
```

표시 필터: `tcp.analysis.retransmission`

### 실습 6 — 링 버퍼로 걸어 둔다

간헐적 장애는 **기다려야** 잡힌다.

```bash
# pc1 — 1MB 짜리 파일 3개를 돌려 쓴다
sudo tcpdump -i eth1 -w /tmp/m07-ring -C 1 -W 3 -nn 'not port 22'
```

```bash
# 다른 창 — 트래픽을 만들어 파일이 넘어가게 한다
ping -f -c 3000 {{ ip.web }}          # flood ping (sudo 필요)
```

```bash
ls -la /tmp/m07-ring*
```

```
/tmp/m07-ring0   1.0M
/tmp/m07-ring1   1.0M
/tmp/m07-ring2   340K
```

> **디스크가 차지 않는다.** 오래된 파일부터 덮어쓰기 때문이다.
> 실무에서는 이걸 며칠씩 걸어 두고, 증상이 난 시각의 파일을 꺼내 본다.
> **`-C` 단위는 MB** 이고, `-W` 를 안 주면 파일이 무한히 늘어난다 — 디스크를 채우는 사고의 단골 원인이다.

### 실습 7 — 캡슐화를 바이트로 짚는다 (A2)

```bash
# pc1
sudo tcpdump -i eth1 -nn -X -c 1 'tcp port 80 and tcp[tcpflags] & tcp-push != 0'
```

```bash
# 다른 창
curl -s -o /dev/null http://{{ ip.web }}/
```

출력의 16진수 덤프에서 아래를 **직접 세어 본다.**

| 찾을 것 | 위치 | 값 |
|---|---|---|
| 목적지 MAC | 0 ~ 5 | `{{ mac.r1 }}` (게이트웨이) 의 바이트들 |
| 출발지 MAC | 6 ~ 11 | `{{ mac.pc1 }}` |
| EtherType | 12 ~ 13 | `0800` (IPv4) |
| IP 버전·헤더길이 | 14 | `45` |
| 출발지 IP | 26 ~ 29 | `{{ ip.pc1 }}` 을 16진수로 |
| 목적지 IP | 30 ~ 33 | `{{ ip.web }}` 을 16진수로 |
| TCP 출발지 포트 | 34 ~ 35 | |
| **페이로드 시작** | **54** | `GET / HTTP/1.1` 의 `G` |

> **`GET` 이 ASCII 열에 그대로 보인다.** 평문 프로토콜이라는 것이 이렇게 드러난다.
> M8 에서 FTP 로그인을 이렇게 잡아 볼 것이다 — **아이디와 비밀번호가 그대로 보인다.**

---

## 4. 관찰 포인트 정리

| 확인할 것 | 방법 | 판단 |
|---|---|---|
| 나가는가 | **출발지**에서 캡처 | 안 나가면 경로·소켓 문제 |
| 도착하는가 | **목적지**에서 캡처 | 도착하면 응답 쪽을 본다 |
| 어디서 사라지나 | **이등분**해서 좁힌다 | 들어오는데 안 나가면 그 장비가 버렸다 |
| 느린 원인 | `-v` 로 재전송 확인 | 같은 seq 반복 = 손실 |
| 태그가 붙었나 | 트렁크에서 `-e` + `vlan` | 태그 유무로 지점을 판별할 수 있다 |
| 같은 패킷인가 | IP ID · TCP seq · ICMP id | **MAC 으로 찾지 말 것** |
| 내 트래픽이 섞였나 | `not port 22` | 없으면 되먹임 |

> **캡처 전에 한 번 멈춰서 물을 것 — "나는 무엇을 증명하려 하는가?"**
> 그 답이 지점과 필터를 동시에 정해 준다. 답이 없으면 아무리 떠도 아무것도 안 나온다.

---

## 5. 장애 주입 챌린지

**이 모듈의 규칙은 다르다 — `show` 명령과 설정 조회를 금지한다.**
**오직 캡처만으로** 원인 구간을 특정해야 한다.

```bash
# 점프 호스트에서 (담당자)
make break LAB={{ lab_id }} SCENARIO=m07-01
```

| # | 증상 | 규칙 |
|---|---|---|
| m07-01 | 통신은 되는데 **가끔 느리다** | 캡처에서 무엇을 찾을 것인가 |
| m07-02 | **pc1 에서만** web HTTP 가 안 된다. ping 은 정상 | **어느 구간에서 사라지는지** 캡처로 특정할 것 |

**제출**: 원인 구간 + **몇 번의 캡처로 찾았는가** + 각 캡처의 지점과 필터.

```bash
make fix LAB={{ lab_id }} SCENARIO=m07-01
```

> **담당자에게**: 이전 모듈의 시나리오(`m03-01`, `m04-02`, `m06-01` …)를 무작위로 하나 더 주입하고
> **"캡처만으로 찾아라"** 를 붙이면 좋은 복습이 된다. 이미 아는 장애를 **다른 도구로** 찾는 훈련이다.

---

## 6. 흔한 오해 · 실무 팁

| 오해 | 사실 |
|---|---|
| "tcpdump 는 필터를 잘 쓰는 것" | 필터는 검색하면 나온다. **지점 선정**이 실력이다. |
| "캡처에 안 보이면 안 지나간 것" | 미러링은 누락될 수 있다. 필터가 틀렸을 수도 있다(트렁크의 `vlan`). |
| "목적지부터 보면 된다" | 안 나갔는지 중간에 사라졌는지 **구분이 안 된다.** 출발지부터. |
| "홉을 순서대로 확인" | **이등분**이 훨씬 빠르다. |
| "MAC 으로 같은 패킷을 찾는다" | MAC 은 홉마다 바뀐다. **IP ID · seq** 로 찾는다. |
| "`-s 0` 을 붙여야 한다" | 요즘 tcpdump 는 기본이 전체 저장이다. 오히려 **줄이는** 경우가 있다. |
| "pcap 은 그냥 로그 파일" | **평문 자격증명이 들어 있다.** 반출은 승인받고, 필요하면 snaplen 을 줄인다. |
| "화면으로 보면 충분" | 지나가면 끝이다. **`-w` 로 뜨고 `-r` 로 여러 번 거른다.** |

**실무 팁**

- 캡처 전에 **`-nn` 과 `not port 22`** 를 반사적으로 친다.
- **넓게 떠서 저장하고, 좁히는 것은 `-r` 로** 한다. 다시 재현시키는 비용이 훨씬 크다.
- 여러 지점을 뜰 때는 **시계를 먼저 확인**한다 (NTP). 시각이 틀어지면 대조가 불가능하다.
- 간헐 장애는 **링 버퍼로 걸어 두고 기다린다.** `-W` 를 빼먹으면 디스크가 찬다.
- 파일 이름에 **지점과 시각**을 넣는다: `pc1-eth1-1430.pcap`. 나중의 자신을 위한 것이다.
- 실장비 대응:

  | 이 랩 | 실장비 |
  |---|---|
  | 각 노드에서 `tcpdump` | 포트 미러링(SPAN) · TAP |
  | `tcpdump -w` | Cisco `monitor capture` · 방화벽의 패킷 캡처 기능 |
  | `-i any` | (장비마다 다름) |

---

> ### 여기서 끝이 아니다
>
> **[과제](#tasks)** 와 **[확인 문제](#quiz)** 를 풀어야 이 모듈이 끝난다.
> 화면 위쪽의 같은 이름 탭에서 열린다.

## 다음

**M8 — 응용 계층.** DNS · HTTP · FTP 를 다룬다.

그리고 이번 모듈에서 익힌 캡처 실력을 바로 쓴다 —
**FTP 로그인 패킷에서 아이디와 비밀번호를 눈으로 읽어 낼 것이다.**
