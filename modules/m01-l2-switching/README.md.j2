<!-- 자동 생성. 원본: modules/m01-l2-switching/README.md.j2  ·  생성: make modules -->

# M1. 랜선 한 가닥부터 — L1/L2와 스위칭

> **소요 시간** {{ meta.duration }} · **랩 단계** `{{ stage }}` · **역량** {{ meta.competencies | join(', ') }}

이 모듈에서는 IP 를 거의 다루지 않습니다. 그 아래 층 — **케이블과 MAC 주소**의 세계입니다.
네트워크 문제의 상당수가 여기서 끝납니다. 그리고 신입이 가장 자주 건너뛰는 층이기도 합니다.

## 학습 목표

{% for o in meta.objectives %}
{{ loop.index }}. {{ o }}
{% endfor %}

## 사전 지식 체크 (M0 복습)

먼저 답해 보고 막히면 M0 로 돌아가 주세요.

- `{{ mgmt_if }}` 와 `eth1` 의 차이는?
- `ping {{ mgmt.pc2 }}` 가 성공했습니다. 이것으로 무엇을 알 수 있고, 무엇을 알 수 없나?
- 접속이 끊겼을 때 들어가는 두 번째 경로는?

---

## 1. 이 단계의 랩

```mermaid
{{ topology }}
```

| 노드 | 역할 | 주소 |
|---|---|---|
| `pc1` | PC | `{{ ipc.pc1 }}` |
| `pc2` | PC | `{{ ipc.pc2 }}` |
| `sw1` | 스위치 | **없음 (L2 전용)** |

> **주목**: 두 PC 가 **같은 대역**(`{{ cidr.vlan10 }}`)에 있습니다. 라우터가 없어도 통신이 됩니다.
> M2 에서 부서가 나뉘며 이 둘을 갈라놓을 것이고, 그때 통신이 끊깁니다. 그 순간이 L3 를 배우는 지점입니다.

> **랙에는 더 있다**: `r1` 도 이미 전원이 켜져 있고 sw1 과 랜선으로 연결돼 있습니다.
> 다만 **아무 설정도 없습니다.** 실습 4 에서 이 "설정 없는 장비"를 관찰 도구로 씁니다.

---

## 2. 개념

### 2.1 L1 — "링크가 살아 있다"는 것

케이블을 꽂으면 두 장비가 전기적으로 서로를 인식합니다. 이것이 **캐리어(carrier)** 입니다.
리눅스는 이걸 인터페이스 플래그로 보여줍니다.

| 상태 | 플래그 | 의미 | 원인 |
|---|---|---|---|
| 정상 | `<...,UP,LOWER_UP>` `state UP` | 켜져 있고 상대도 살아 있음 | — |
| 관리적 down | `<BROADCAST,MULTICAST>` `state DOWN` | **내가 꺼놨습니다** | `ip link set ... down`, 장비 설정 |
| 물리적 down | `<NO-CARRIER,...,UP>` `state DOWN` | 켜져 있는데 **상대가 없습니다** | 케이블 빠짐/불량, 상대 포트 down |

> **이 구분이 실무 1차 판정입니다.**
> `NO-CARRIER` 면 케이블·SFP·상대 포트를 봅니다. 플래그에 `UP` 이 없으면 내 설정을 봅니다.
> 여기서 갈리는데 무작정 `ping` 부터 치면 30분을 버립니다.

**카운터**도 L1 의 증거입니다.

| 카운터 | 의미 | 의심할 것 |
|---|---|---|
| `RX errors` / `CRC` | 깨진 프레임 수신 | 케이블 불량, 노이즈, 커넥터 |
| `TX errors` | 송신 실패 | 링크 품질, 드라이버 |
| `dropped` | 버퍼에서 버림 | 과부하, 큐 부족 |
| `RX/TX packets` 가 0 | 아예 오간 게 없음 | 배선이 딴 데 꽂힘 |

> **에러 카운터는 "값"이 아니라 "증가율"로 봅니다.**
> 총 10개는 무의미합니다. 지금 1분에 100개씩 늘고 있다면 그게 장애입니다.

### 2.2 L2 — 이더넷 프레임과 MAC 주소

같은 세그먼트 안에서 데이터는 **프레임(frame)** 단위로 오갑니다.

```
┌─────────────┬─────────────┬────────┬──────────────────┬─────┐
│ 목적지 MAC   │ 출발지 MAC   │ 타입    │      데이터        │ FCS │
│   6바이트    │   6바이트    │ 2바이트 │  (IP 패킷 등)     │     │
└─────────────┴─────────────┴────────┴──────────────────┴─────┘
```

MAC 주소는 `{{ mac.pc1 }}` 같은 48비트 값이고, **NIC 에 붙어 있습니다.**

| | MAC 주소 | IP 주소 |
|---|---|---|
| 붙어 있는 곳 | NIC(하드웨어) | 설정(소프트웨어) |
| 유효 범위 | **같은 세그먼트 안에서만** | 엔드투엔드 |
| 누가 쓰나 | 스위치 | 라우터 |
| 라우터를 넘으면 | **바뀝니다** | 그대로 |

> 마지막 줄이 L2 와 L3 를 가르는 핵심입니다. M2 에서 패킷으로 직접 확인합니다.

**목적지 MAC 의 종류**

| 종류 | 주소 | 의미 |
|---|---|---|
| 유니캐스트 | `{{ mac.pc2 }}` | 특정 한 대에게 |
| 브로드캐스트 | `ff:ff:ff:ff:ff:ff` | **세그먼트 전체에게** |
| 멀티캐스트 | `01:00:5e:...` 등 | 관심 있는 무리에게 |

### 2.3 스위치는 무엇을 하는가

스위치가 하는 일은 딱 세 가지입니다.

```
① 학습(Learning)   프레임이 들어오면 "출발지 MAC 은 이 포트에 있다"고 적어둔다
② 전달(Forwarding) 목적지 MAC 을 표에서 찾아 그 포트로만 보낸다
③ 플러딩(Flooding) 표에 없으면 들어온 포트를 뺀 모든 포트로 뿌린다
```

여기에 하나 더:

```
④ 에이징(Aging)    일정 시간 조용한 항목은 표에서 지운다 (기본 300초)
```

> **왜 플러딩하는가?**
> 스위치는 처음에 아무것도 모릅니다. 모르는 곳으로 가는 프레임을 버리면 통신이 시작될 수 없습니다.
> 그래서 **모르면 일단 다 뿌리고, 응답이 오면 그때 배웁니다.**
> 이 "모르면 뿌린다"가 실습 4 에서 눈으로 확인할 내용입니다.

### 2.4 ARP — IP 를 MAC 으로 바꾸기

`ping {{ ip.pc2 }}` 를 치면, 보내는 쪽은 **목적지 MAC 을 모릅니다.** IP 밖에 없기 때문입니다.
그래서 먼저 물어봅니다.

```
pc1 ──브로드캐스트──> 세그먼트 전체 :  "{{ ip.pc2 }} 쓰는 사람 누구? {{ ip.pc1 }} 가 묻는다"
pc2 ──유니캐스트────> pc1           :  "나야. 내 MAC 은 {{ mac.pc2 }}"
```

- **Request 는 브로드캐스트** — 누구인지 모르니 전체에게 물을 수밖에 없습니다
- **Reply 는 유니캐스트** — 물어본 사람은 알고 있으니 그 사람에게만

결과는 ARP 테이블(`ip neigh`)에 캐시됩니다. 그래서 두 번째 ping 부터는 ARP 가 없습니다.

> **ARP 는 세그먼트를 넘지 못합니다.** 브로드캐스트이기 때문입니다.
> 이 사실이 M2 에서 "라우터가 왜 필요한가"의 답이 됩니다.

### 2.5 브로드캐스트 도메인 / 충돌 도메인

| | 충돌 도메인 | 브로드캐스트 도메인 |
|---|---|---|
| 무엇 | 동시에 보내면 충돌하는 범위 | 브로드캐스트가 닿는 범위 |
| 허브 | 전체가 하나 | 전체가 하나 |
| **스위치** | **포트마다 분리** | 전체가 하나 |
| **라우터** | 포트마다 분리 | **포트마다 분리** |

> 스위치를 아무리 늘려도 브로드캐스트 도메인은 하나입니다. 이걸 나누려면
> **VLAN(M2)** 이나 **라우터(M2)** 가 필요합니다.

### 2.6 L2 루프와 STP — 왜 알아야 하나

스위치를 두 개의 경로로 연결하면 어떻게 되나?

```
        ┌── sw1 ──┐
   pc1──┤         ├── sw2 ──pc2      ← 경로가 두 개
        └─────────┘
```

브로드캐스트 프레임 하나가 들어오면:
1. sw1 이 모든 포트로 뿌립니다
2. sw2 가 받아서 모든 포트로 뿌립니다
3. 그게 다시 sw1 으로 돌아옵니다
4. **1번으로 돌아갑니다 — 영원히**

IP 패킷에는 TTL 이 있어 루프가 스스로 멈추지만, **이더넷 프레임에는 TTL 이 없습니다.**
그래서 프레임이 무한히 증식합니다. 이것이 **브로드캐스트 스톰**이고, 몇 초 만에 네트워크 전체가 마비됩니다.
같은 MAC 이 포트를 오가며 표가 요동치는 **MAC 플래핑**도 함께 일어납니다.

**STP(Spanning Tree Protocol)** 는 이 루프를 감지해 한쪽 경로를 **논리적으로 차단**합니다.
평소엔 안 쓰다가 주 경로가 죽으면 살립니다.

> 이 랩에서 STP 를 설정하지는 않습니다. 다만 **"L2 에 루프가 생기면 즉시 사고"**라는 감각과
> **"그래서 STP 가 켜져 있다"**는 사실은 반드시 갖고 가야 합니다.
> 실무에서 스위치를 잘못 꽂아 사무실 전체를 마비시키는 사고가 지금도 일어납니다.

---

## 3. 실습

> 노드 3개(`pc1`, `pc2`, `sw1`)와 관찰용 `r1` 을 씁니다. 터미널 창을 여러 개 열어 두세요.

### 실습 1 — 물리 계층부터 본다 (B1)

```bash
# pc1 에서
ip -br link show
```

```
lo               UNKNOWN        00:00:00:00:00:00 <LOOPBACK,UP,LOWER_UP>
{{ "%-16s"|format(mgmt_if) }} UP             {{ nodes.pc1.mgmt_mac }} <BROADCAST,MULTICAST,UP,LOWER_UP>
eth1             UP             {{ mac.pc1 }} <BROADCAST,MULTICAST,UP,LOWER_UP>
```

`UP` 과 `LOWER_UP` 이 **둘 다** 있어야 정상입니다.

```bash
# 카운터
ip -s link show eth1
```

```
2: eth1: <BROADCAST,MULTICAST,UP,LOWER_UP> mtu 1500 ...
    link/ether {{ mac.pc1 }} brd ff:ff:ff:ff:ff:ff
    RX:  bytes packets errors dropped  missed   mcast
          1234      15       0       0       0       0
    TX:  bytes packets errors dropped carrier collsns
          1102      13       0       0       0       0
```

**errors 와 dropped 가 0 인지** 봅니다. 0 이 아니면 그 자체가 단서입니다.

```bash
# 관리적으로 내려 본다
sudo ip link set eth1 down
ip -br link show eth1
```

```
eth1             DOWN           {{ mac.pc1 }} <BROADCAST,MULTICAST>
```

`UP` 플래그가 사라졌다 = **내가 껐다**는 뜻. `NO-CARRIER` 는 없습니다.

```bash
sudo ip link set eth1 up
```

> **가상 환경의 한계**: 이 랩은 VM 이라 상대 포트를 내려도 내 쪽에 `NO-CARRIER` 가 뜨지 않습니다.
> 실장비에서는 뜹니다. `ethtool eth1` 의 Speed/Duplex 도 virtio NIC 에서는 `Unknown` 으로 나옵니다.
> **실장비에서는 여기서 speed/duplex 불일치를 잡는다**는 것만 기억해 두세요.

### 실습 2 — 첫 통신과 ARP (B4)

**터미널 2 (pc2)** — 캡처를 먼저 걸어 둡니다.

```bash
sudo tcpdump -i eth1 -e -n arp or icmp
```

**터미널 1 (pc1)**

```bash
sudo ip neigh flush dev eth1     # ARP 캐시를 비운다
ip neigh show dev eth1           # 비었는지 확인
ping -c 1 {{ ip.pc2 }}
```

**터미널 2 에 보이는 것**

```
{{ mac.pc1 }} > ff:ff:ff:ff:ff:ff, ethertype ARP (0x0806), length 42:
    Request who-has {{ ip.pc2 }} tell {{ ip.pc1 }}, length 28
{{ mac.pc2 }} > {{ mac.pc1 }}, ethertype ARP (0x0806), length 42:
    Reply {{ ip.pc2 }} is-at {{ mac.pc2 }}, length 28
{{ mac.pc1 }} > {{ mac.pc2 }}, ethertype IPv4 (0x0800), length 98:
    {{ ip.pc1 }} > {{ ip.pc2 }}: ICMP echo request, ...
{{ mac.pc2 }} > {{ mac.pc1 }}, ethertype IPv4 (0x0800), length 98:
    {{ ip.pc2 }} > {{ ip.pc1 }}: ICMP echo reply, ...
```

**관찰 포인트**

1. 첫 줄의 목적지가 `ff:ff:ff:ff:ff:ff` — **브로드캐스트**다
2. 둘째 줄은 `{{ mac.pc1 }}` 로 직접 — **유니캐스트**다
3. ICMP 는 ARP 가 끝난 **다음에야** 나갑니다
4. 한 번 더 ping 하면 **ARP 가 없습니다** (캐시)

```bash
# pc1 — 캐시 확인
ip neigh show dev eth1
```

```
{{ ip.pc2 }} lladdr {{ mac.pc2 }} REACHABLE
```

> **`ip neigh` 는 L2 도달성의 증거입니다.**
> 여기에 상대가 `REACHABLE` 로 있으면 최소한 **같은 세그먼트에는 살아 있다**는 뜻입니다.
> `FAILED` 나 `INCOMPLETE` 면 ARP 응답이 안 온 것 — L2 부터 문제입니다.

### 실습 3 — 스위치가 무엇을 알고 있나 (B2)

```bash
# sw1 에서
bridge fdb show br br0 | grep -v permanent
```

```
{{ mac.pc1 }} dev eth1 master br0
{{ mac.pc2 }} dev eth2 master br0
```

**이것이 MAC 주소 테이블입니다.** 실장비의 `show mac address-table` 과 같습니다.

읽는 법: "`{{ mac.pc1 }}` 라는 장비는 `eth1` 포트 쪽에 있습니다."

```bash
# 마지막 사용 시각까지
bridge -s fdb show br br0 | grep -v permanent
```

```
{{ mac.pc1 }} dev eth1 master br0 used 3/3
```

`used A/B` = A초 전에 사용, B초 전에 갱신.

```bash
# 포트 상태
bridge link show
```

> **`permanent` 항목은 스위치 자기 포트의 MAC 입니다.** 학습된 것이 아니므로 걸러서 봅니다.

### 실습 4 — 플러딩을 눈으로 증명한다 ★ (B2)

이 실습이 M1 의 핵심입니다. **관찰용 세 번째 포트**를 만듭니다.

`r1` 은 이미 sw1 과 랜선으로 연결돼 있습니다. 설정은 없지만 **프레임은 받을 수 있습니다.**

```bash
# sw1 — 아직 안 쓰는 포트를 스위치에 꽂는다
sudo ip link set eth3 up
sudo ip link set eth3 master br0
bridge link show          # eth3 가 목록에 나타난다
```

```bash
# r1 (터미널 3) — 설정 없는 장비로 엿듣기
sudo ip link set eth1 up
sudo tcpdump -i eth1 -e -n
```

#### 실험 A — 스위치가 아는 목적지

```bash
# pc1
ping -c 2 {{ ip.pc2 }}
```

**r1 에 보이는 것**: 거의 없습니다. ARP 가 필요했다면 브로드캐스트만 보이고,
**ICMP 는 한 줄도 보이지 않습니다.**

> sw1 은 `{{ mac.pc2 }}` 가 `eth2` 에 있다는 걸 압니다. 그래서 `eth2` 로만 보냅니다.
> `eth3`(r1) 로는 보낼 이유가 없습니다.

#### 실험 B — 스위치가 모르는 목적지

존재하지 않는 장비에게 보냅니다.

```bash
# pc1 — 있지도 않은 MAC 을 목적지로 강제 지정
sudo ip neigh replace {{ unused_ip.vlan10 }} lladdr {{ fake_mac }} dev eth1
ping -c 3 {{ unused_ip.vlan10 }}
```

**r1 에 보이는 것**

```
{{ mac.pc1 }} > {{ fake_mac }}, ethertype IPv4 (0x0800), length 98:
    {{ ip.pc1 }} > {{ unused_ip.vlan10 }}: ICMP echo request, ...
```

> **r1 은 자기 것이 아닌 프레임을 받았습니다.**
> 목적지 MAC 이 `{{ fake_mac }}` 인데 r1 의 MAC 이 아닙니다. 그런데도 도착했습니다.
> **이것이 플러딩의 직접 증거입니다.**

```bash
# sw1 — 이 MAC 을 학습했나?
bridge fdb show br br0 | grep de:ad
```

아무것도 안 나옵니다. **응답하는 장비가 없으니 학습할 수 없고, 그래서 매번 플러딩합니다.**

> **정리**: 스위치는 아는 목적지에는 해당 포트로만, 모르는 목적지에는 모든 포트로 보냅니다.
> 브로드캐스트는 언제나 모든 포트로 갑니다.
> 실험 A 와 B 의 차이가 그것입니다.

**정리 (다음 실습을 위해)**

```bash
# pc1
sudo ip neigh del {{ unused_ip.vlan10 }} dev eth1
```

### 실습 5 — 에이징 (B2)

```bash
# sw1 — 현재 에이징 타임 (단위: 1/100초. 30000 = 300초)
ip -d link show br0 | grep -o 'ageing_time [0-9]*'
```

관찰하기 좋게 15초로 줄입니다.

```bash
sudo ip link set br0 type bridge ageing_time 1500
```

```bash
# pc1 에서 한 번 통신
ping -c 1 {{ ip.pc2 }}

# sw1 에서 반복 확인
watch -n 2 'bridge -s fdb show br br0 | grep -v permanent'
```

`used` 값이 올라가다가 **15초가 넘으면 항목이 사라집니다.**
그 뒤 다시 통신하면 플러딩부터 다시 시작합니다.

```bash
# 원복
sudo ip link set br0 type bridge ageing_time 30000
```

> **왜 지우는가?** 장비가 다른 포트로 옮겨가도 따라가야 하고, 표가 무한정 커지면 안 되기 때문입니다.
> 실무 팁: 통신은 되는데 **간헐적으로 느려지는** 증상의 원인이 에이징과 플러딩인 경우가 있습니다.

### 실습 6 — 브로드캐스트 도메인 (A3)

```bash
# pc2 (터미널 2)
sudo tcpdump -i eth1 -e -n 'ether broadcast'
```

```bash
# pc1 — pc2 와 무관한 주소를 향해 ARP 를 쏜다
sudo arping -c 3 -I eth1 {{ unused_ip.vlan10 }}
```

pc2 는 **자기와 상관없는 ARP 요청**을 전부 받습니다.
`r1` 에서도 똑같이 보입니다. **셋이 하나의 브로드캐스트 도메인**에 있기 때문입니다.

> M2 에서 VLAN 으로 이 도메인을 둘로 쪼갭니다. 그러면 이 ARP 는 더 이상 pc2 에 도착하지 않습니다.

---

## 4. 관찰 포인트 정리

| 확인할 것 | 명령 | 정상이라면 |
|---|---|---|
| 링크가 살아 있나 | `ip -br link show` | `UP` + `LOWER_UP` |
| 물리적 문제가 있나 | `ip -s link show eth1` | errors / dropped 가 **증가하지 않음** |
| 상대가 같은 세그먼트에 있나 | `ip neigh show` | 상대가 `REACHABLE` |
| 스위치가 상대를 아는가 | `bridge fdb show br br0` | 상대 MAC 이 올바른 포트에 |
| 실제로 프레임이 오갔나 | `tcpdump -i eth1 -e -n` | 요청과 응답이 쌍으로 |

> **순서가 중요합니다.** 위에서부터 봅니다. 링크가 죽었는데 `tcpdump` 를 켜는 것은 시간 낭비입니다.

---

## 5. 장애 주입 챌린지

교육 담당자가 아래 중 하나를 주입합니다. **증상만 보고 원인을 찾아라.**

```bash
# 점프 호스트에서 (담당자)
make break LAB={{ lab_id }} SCENARIO=m01-01
```

| # | 증상 | 힌트 |
|---|---|---|
| m01-01 | pc1 → pc2 ping 무응답 | 실습 1 의 표를 다시 볼 것 |
| m01-02 | ping 무응답. **링크는 양쪽 다 UP** | 링크가 살아 있으면 통신이 되는가? |
| m01-03 | ping 무응답. 링크도 UP, `bridge fdb` 도 정상, `ip neigh` 에 상대가 **등록되어 있습니다** | 등록돼 있는 것과 **맞는 것**은 다릅니다 |

**규칙**: 복구하기 전에 **원인을 먼저 말합니다.** 근거(명령어 출력·캡처)를 함께 제시해 주세요.

복구:

```bash
make fix LAB={{ lab_id }} SCENARIO=m01-01
```

---

## 6. 흔한 오해 · 실무 팁

| 오해 | 사실 |
|---|---|
| "스위치에도 IP 가 있어야 한다" | L2 스위치는 전달에 IP 가 필요 없습니다. 관리용 IP 는 **관리하려고** 붙이는 것입니다. |
| "MAC 주소는 절대 안 바뀐다" | NIC 고유값이지만 소프트웨어로 바꿀 수 있습니다. 그래서 중복 MAC 사고가 납니다. |
| "ping 이 되면 L2 는 정상" | 맞습니다. 다만 **역은 성립하지 않습니다** — ping 이 안 돼도 L2 는 멀쩡할 수 있습니다(방화벽 등). |
| "플러딩은 비정상" | 정상 동작입니다. 다만 **계속** 플러딩되면 비정상(에이징·MAC 플래핑 의심). |
| "링크가 UP 이면 문제없다" | UP 이어도 CRC 에러가 쌓이면 통신 품질이 무너집니다. **카운터를 봅니다.** |

**실무 팁**

- 장애 신고를 받으면 **`ip -br link` 부터** 봅니다. 30초면 L1 문제를 걸러낼 수 있습니다.
- 카운터는 두 번 찍어서 **차이**를 봅니다. `ip -s link show eth1` 을 10초 간격으로.
- `bridge fdb` 에서 **같은 MAC 이 포트를 오가면** MAC 플래핑 — 루프를 의심합니다.
- 실장비 명령 대응:
  | 이 랩 | Cisco IOS |
  |---|---|
  | `bridge fdb show br br0` | `show mac address-table` |
  | `bridge link show` | `show interfaces status` |
  | `ip -s link show eth1` | `show interfaces GigabitEthernet0/1` |
  | `ip neigh show` | `show arp` |

---

> ### 여기서 끝이 아닙니다
>
> 위쪽 탭에서 **[퀴즈](#quiz) → [과제](#tasks) → [검증](#verify)** 순으로 진행합니다.
> 퀴즈로 개념을 확인하고, 과제에서 랩에 직접 만들고, 검증에서 그 결과를 기계가 봅니다.

## 다음

**M2 — IP 주소와 L3 의 등장.** 지금 잘 통하는 두 PC 를 VLAN 으로 갈라놓습니다.
통신이 끊길 것이고, 그 끊긴 자리에 라우터가 들어옵니다.
