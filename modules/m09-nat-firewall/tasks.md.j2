{% set dweb = public.nat_pool.dnat | selectattr('node','equalto','web') | first %}
{% set dftp = public.nat_pool.dnat | selectattr('node','equalto','ftp') | first %}
<!-- 자동 생성. 원본: modules/m09-nat-firewall/tasks.md.j2 -->

# M9 과제

## 실습 과제

### 과제 1. ★★ 주소가 바뀌는 순간을 캡처로 증명
경계 장비 **양쪽**에 동시에 캡처를 걸고 SNAT 와 DNAT 를 각각 증명합니다.

```bash
# edge 에서 저장
sudo tcpdump -i eth1 -nn -w /tmp/m09-inside.pcap  'not port 22'
sudo tcpdump -i eth2 -nn -w /tmp/m09-outside.pcap 'not port 22'
```

**(1) SNAT** — pc1 에서 `{{ public.external_site.inet_loopback }}` 으로 접속하고 표를 채웁니다.

| | 출발지 IP | 출발지 포트 | 목적지 IP | 목적지 포트 |
|---|---|---|---|---|
| 안쪽 (`eth1`) | | | | |
| 바깥쪽 (`eth2`) | | | | |
| **무엇이 바뀌었나** | | | | |

**(2) DNAT** — inet 에서 `{{ dweb.public }}` 으로 접속하고 같은 표를 채웁니다.

| | 출발지 IP | 출발지 포트 | 목적지 IP | 목적지 포트 |
|---|---|---|---|---|
| 바깥쪽 (`eth2`) | | | | |
| 안쪽 (`eth1`) | | | | |
| **무엇이 바뀌었나** | | | | |

**(3)** 두 표를 비교해 **SNAT 과 DNAT 의 차이**를 한 문장으로 쓰시오.

**(4)** `conntrack -L` 출력에서 (1) 의 세션 한 줄을 찾아 붙여 넣고,
**변환 전 값과 변환 후 값이 각각 어디에 적혀 있는지** 표시하시오.

---

### 과제 2. ★ Stateful 증명
**(1)** 경계 정책에서 **"외부 → 내부" 를 허용하는 규칙이 몇 개**인지 세시오.
그 규칙들이 허용하는 것은 무엇인가?

**(2)** pc1 이 외부 사이트에서 응답을 받습니다. **어느 규칙 덕분인가?**
그 규칙을 그대로 옮겨 적어 주세요.

**(3)** **[교재]** 탭의 실습 3(세션 지우기)을 재현하고 기록합니다.

| 단계 | 무엇을 했나 | 결과 |
|---|---|---|
| 연결을 열어 둠 | | |
| `conntrack -F` 실행 | | |
| 열어 둔 연결로 요청 | | |
| **새 연결 시도** | | |

**(4)** 왜 열어 둔 연결만 죽고 새 연결은 되는가? 두 문장으로.

**(5)** 이 현상이 실무에서 나타나는 상황을 **하나** 들고, 그때의 특징적인 증상을 쓰시오.
(힌트: 장비 재기동 · 이중화 전환)

---

### 과제 3. 음성 검사 — 되면 안 되는 것
**"되는 것" 만 확인하는 검증은 절반짜리입니다.** 되면 안 되는 것도 확인합니다.

`inet` 에서 아래를 시도하고 **결과와 실패의 성격**을 적습니다.

| # | 시도 | 결과 | 실패의 성격 (경로 없음 / DROP / refused / 성공) | 어디서 막혔나 |
|---|---|---|---|---|
| 1 | `ping {{ ip.web }}` (사설) | | | |
| 2 | `ping {{ dweb.public }}` (공개) | | | |
| 3 | `{{ dweb.public }}:80` 접속 | | | |
| 4 | `{{ dweb.public }}:22` 접속 | | | |
| 5 | `ping {{ ip.pc1 }}` (내부 PC) | | | |

**질문**

- 1번과 4번은 **둘 다 실패**하지만 성격이 완전히 다릅니다. 무엇이 다른가?
- 1번이 실패하는 진짜 이유는 방화벽인가? 근거는?
- 5번이 실패하는 이유를 **두 가지** 쓰시오 (둘 다 NAT 가 아닙니다).
- 이 표에서 **하나라도 "성공" 이 나오면** 어떤 의미인가?

---

### 과제 4. FTP 와 NAT
**(1)** `227` 응답을 캡처에서 찾아 붙여 넣고, 괄호 안의 **IP 와 포트**를 해독하시오.

**(2)** 그 IP 는 공개 IP 인가 사설 IP 인가? **누가 그렇게 만들었는가?**

**(3)** Active 모드로도 한 번 접속해 같은 자리에서 캡처합니다.

```bash
# ftp 클라이언트에서 passive 를 끄고 접속한다
ftp -A {{ ip.ftp }}
```

그 데이터 채널의 SYN 은 **서버가 클라이언트에게** 겁니다.
그 SYN 이 이 경계에 도착하면 어느 규칙에 걸리는지 `nft list ruleset` 을 보며 답하시오.

**(4)** 아래 두 상황에서 **깨지는 모드**와 **해결책**을 각각 쓰시오.

| 상황 | 깨지는 모드 | 해결 |
|---|---|---|
| 서버가 NAT 뒤 (이 랩) | | |
| 클라이언트가 NAT 뒤 (일반 회사) | | |

**(5)** 데이터 포트 대역(`{{ services.ftp.passive_range }}`)을 고정하지 않으면
방화벽 정책을 쓸 수 없는 이유를 한 문장으로.

---

### 과제 5. 정책 설계
새 요구사항이 내려왔습니다. **경계 정책을 어떻게 바꿀지** 규칙으로 쓰시오.
(문법은 완벽하지 않아도 됩니다. **순서와 방향**이 맞아야 합니다)

| # | 요구사항 |
|---|---|
| 1 | 내부에서 외부로 나가는 것 중 **FTP(21)만 금지**합니다 |
| 2 | 외부에서 `{{ dweb.public }}` 의 443 도 열어 줍니다 |
| 3 | 특정 외부 IP `{{ public.transit.addresses.inet }}` 는 **모든 접근을 차단**합니다 |
| 4 | 내부 → 외부 중 서버망(`{{ cidr.vlan40 }}`)만 **DNS(53) 허용, 나머지 금지** |

**작성한 규칙을 순서대로 나열하고, 각 줄마다 "왜 이 위치인가" 를 한 줄로 적어 주세요.**

**질문**

- 3번 규칙을 맨 아래에 두면 왜 동작하지 않는가?
- 4번에서 "나머지 금지" 를 위해 규칙을 하나 더 써야 하는가? chain 의 `policy` 와 어떤 관계인가?
- 리턴 트래픽을 위한 규칙을 추가로 써야 하는가? 왜인가?


### 과제 6. 장애 하나를 골라 스스로 찾기 ★

**언제** — 위 과제를 다 끝낸 지금입니다. 설정이 손에 익은 뒤라야 증상이 보입니다.

**왜** — 지금까지는 *무엇을 만들지 알고* 만들었습니다. 실무는 반대입니다 —
증상 하나만 주어지고, 무엇이 고장인지는 아무도 알려주지 않습니다.
그 방향을 여기서 한 번 뒤집어 봅니다.

**어떻게**

1. 실행 패널의 **[장애 연습]** 에서 시나리오를 하나 고르고 **[주입]** 을 누릅니다
2. 증상만 보고 원인을 찾습니다. 증상표와 힌트는 **[교재]** 탭의 [5. 장애 연습](#5-장애-연습-—-증상만-보고-원인-찾기) 에 있습니다
3. 고치기 전에 **원인을 먼저 말합니다.** 근거가 된 명령 출력을 남겨 두세요
4. **[복구]** 로 되돌립니다. 엉켰으면 **[이 모듈 적용]** 이 확실합니다

> 여기서는 **무엇을 넣었는지 알고** 시작합니다. 무엇인지 가려 놓고 푸는 자리는
> 따로 있습니다 — 그때를 위한 준비가 이것입니다.

**여기서 남긴 기록이 [검증] 의 서술 과제 «장애 원인 서술 — 경계에서 무슨 일이 일어났는가 ★» 입니다.**
그 지문은 *주입한 장애 하나를 골라* 로 시작합니다 — 넣어 본 적이 없으면 쓸 것이 없습니다.


## 자가 점검

다음을 **막힘 없이** 할 수 있으면 M10 으로 넘어가도 됩니다.

- [ ] 경계 장비의 **양쪽 인터페이스**에 동시에 캡처를 걸어 변환을 확인한다
- [ ] `conntrack -L` 에서 변환 전후를 읽는다
- [ ] "리턴 트래픽 규칙은 왜 없는가" 에 답한다
- [ ] **NAT 는 보안 기능이 아니다** 를 증거와 함께 말한다
- [ ] 실패까지 걸린 **시간**으로 경로 없음 / DROP / refused 를 가른다
- [ ] 정책의 **순서와 방향**을 의식하며 규칙을 쓴다
- [ ] FTP 가 NAT 에서 깨지는 지점을 `227` 응답으로 지목한다
- [ ] **되면 안 되는 것이 안 되는지** 확인하는 습관을 갖는다

---

## 어떻게 제출하나

통과 조건은 셋입니다.

| 무엇 | 어디서 | 무엇을 보나 |
|---|---|---|
| 퀴즈 | **[퀴즈]** 탭 | 개념을 아는가 |
| 검증 | **[검증]** 탭 | 랩이 이 모듈의 정상 상태인가 |
| 장애 실습 | 실행 패널 **[주입]** → 고친 뒤 **[검증]** | 고장 하나를 스스로 찾아 고쳤는가 |

서술 과제는 **[검증]** 탭에서 함께 냅니다. 관리자가 승인해야 다음 모듈이 열린다.

**[검증]** 은 "네가 만들었는가" 를 묻지 않는다. 랩이 정상인지 볼 뿐이라
**[이 모듈 적용]** 직후에도 통과한다. 진단을 실제로 해 보는 것은 장애 실습이다.

> 주의 — 장애 실습은 **[복구]** 나 **[이 모듈 적용]** 으로 되돌리면 인정되지 않는다.
> 정답을 본 것이지 찾은 것이 아니기 때문이다. 막히면 **[힌트]** 를 쓰세요.
