<!-- 자동 생성. 원본: modules/m00-orientation/README.md.j2  ·  생성: make modules -->

# M0. 오리엔테이션 — 접속하고, 망가뜨리고, 되돌리기

> **소요 시간** {{ meta.duration }} · **랩 단계** `{{ stage }}`
>
> 이 모듈에서는 네트워크를 배우지 않는다. **랩을 다루는 법**을 배운다.
> 특히 마지막 실습에서 일부러 접속을 끊어볼 것이다. 그게 이 모듈의 핵심이다.

## 학습 목표

{% for o in meta.objectives %}
{{ loop.index }}. {{ o }}
{% endfor %}

---

## 1. 이 랩은 어떻게 생겼나

최종적으로 이런 네트워크를 직접 만들게 된다.

```mermaid
{{ topology_full }}
```

지금(M0·M1 단계)은 여기까지만 있다. **모듈이 진행되면서 장비가 하나씩 늘어난다.**

```mermaid
{{ topology }}
```

> 장비를 처음부터 다 주지 않는 이유가 있다.
> "왜 이 장비가 필요한가"는 **없어서 불편해진 순간**에 가장 잘 이해되기 때문이다.
> 지금은 PC 두 대와 스위치 한 대뿐이다.

---

## 2. 두 개의 네트워크 ★ 이 모듈에서 가장 중요한 내용

랩의 모든 노드는 **네트워크에 두 번 연결**되어 있다.

```
                     ┌──────────────────────────────┐
   당신의 PC ────────│  관리망 (Out-of-Band)        │
                     │  {{ "%-20s"|format(mgmt_cidr) }}      │  ← 항상 살아 있다
                     │  인터페이스: {{ "%-10s"|format(mgmt_if) }}      │
                     └──────────┬───────────────────┘
                                │
                          ┌─────┴─────┐
                          │    pc1    │
                          └─────┬─────┘
                                │
                     ┌──────────┴───────────────────┐
                     │  서비스망 (실습 대상)          │
                     │  {{ "%-20s"|format(lab_block) }}      │  ← 여기를 망가뜨린다
                     │  인터페이스: eth1, eth2, ...   │
                     └──────────────────────────────┘
```

| | 관리망 | 서비스망 |
|---|---|---|
| 인터페이스 | `{{ mgmt_if }}` | `eth1`, `eth2`, … |
| 대역 | `{{ mgmt_cidr }}` | `{{ lab_block }}` |
| 용도 | **접속 유지 전용** | **실습 대상** |
| 실습 중 건드리나 | ✗ 절대 | ○ 마음껏 |
| 랩이 망가지면 | 여전히 살아 있음 | 죽음 |

### 실무에서는 이것을 무엇이라 부르나

실장비에도 똑같은 것이 있다. **콘솔 포트**와 **관리 포트(mgmt0 / me0 / management)** 다.
장비의 라우팅 설정을 잘못해서 통신이 끊겨도, 엔지니어는 콘솔로 들어가 되돌린다.
**관리 경로를 서비스 경로와 분리해 두는 것**은 네트워크 운영의 기본 원칙이다.

### ⚠ 함정 — 이 랩에서 가장 많이 하는 실수

```
"ping 이 되니까 통신은 정상이네요"   ← 관리망 IP 로 ping 했다면 틀렸다
```

관리망은 랩 설정과 **무관하게 항상 통한다.**
라우팅을 다 지워도, VLAN 을 엉망으로 만들어도 `{{ mgmt_if }}` 는 멀쩡하다.

> **규칙: 모든 통신 확인은 `eth1` 이후의 서비스망 주소로 한다.**
> 관리망 주소({{ mgmt_cidr }})가 나오면 그 검증은 무효다.

---

## 3. 접속하는 두 가지 방법

| | ① 콘솔 (Proxmox 웹) | ② SSH (점프 호스트 경유) |
|---|---|---|
| 무엇 | 브라우저에서 화면 직접 접속 | 터미널 |
| 실장비 대응 | **콘솔 케이블 / 터미널 서버** | 원격 접속 |
| 네트워크가 다 죽어도 | **○ 된다** | ✗ 안 된다 |
| 복붙·파일 전송 | 불편 | 편함 |
| 평소 | 비상용 | **주력** |

### ① 콘솔

브라우저 → `{{ console_url }}` → 왼쪽에서 VM 선택 → **Console**

콘솔은 **키를 쓸 수 없다.** 화면에 직접 붙는 경로라 비밀번호로만 들어간다.
그래서 두 가지가 따로 필요하다.

| 무엇 | 어디서 받나 |
|---|---|
| Proxmox 로그인 계정 | 교육 담당자가 발급 (Realm 은 *Proxmox VE authentication server*) |
| 노드의 `{{ lab_user }}` 계정 비밀번호 | 웹 콘솔 **[접속 키] → 5. 콘솔** |

평소에는 쓸 일이 없다. **SSH 가 죽었을 때만** 쓴다.

### ② SSH

배포받은 SSH 설정을 `~/.ssh/config` 에 넣으면 노드 이름만으로 접속된다.

```bash
ssh pc1
```

내부적으로는 점프 호스트(`{{ jump_ip }}`)를 거쳐 `{{ mgmt.pc1 }}` 로 들어간다.

---

## 4. 실습

> 모든 실습은 **복붙 가능**하다. 예상 출력과 다르면 그 자리에서 멈추고 확인할 것.

### 실습 1 — 접속 준비 (키 만들고 등록하기)

랩 노드에는 **비밀번호가 없다.** SSH 키로만 들어간다.
불편하게 하려는 게 아니라 실무 기본값이다 — 키는 개인키 없이는 쓸 수 없고,
누가 언제 들어왔는지가 계정별로 남는다.

**터미널을 연다.** Windows 는 **PowerShell** 을 쓴다 —
시작 메뉴에서 `PowerShell` 을 검색해 실행한다. (설치할 것은 없다. Windows 10 이상에는
SSH 가 기본으로 들어 있다.)

> `ssh : 용어가 인식되지 않습니다` 가 나오면 SSH 가 꺼져 있는 것이다.
> **설정 → 시스템 → 선택적 기능 → 기능 추가** 에서 `OpenSSH 클라이언트` 를 켠다.

**1) 내 PC 에서 키를 만든다** (이미 있으면 건너뛴다)

```powershell
ssh-keygen -t ed25519 -C "내이름@내PC"
```

세 번 묻는다. **전부 그냥 Enter** 를 친다 (저장 위치 기본값, 암호 없음).

```powershell
Get-Content $HOME\.ssh\id_ed25519.pub
```

`ssh-ed25519 AAAA...` 로 시작하는 **한 줄**이 나온다. 그게 공개키다.

> 같은 폴더에 이름이 비슷한 파일이 둘 있다.
> `id_ed25519.pub` 는 **공개키** — 남에게 줘도 되는 자물쇠다.
> `id_ed25519` (`.pub` 없음) 는 **개인키** — 누구에게도 주지 않는 열쇠다.
> **둘을 바꿔 내는 것이 이 바닥에서 가장 흔한 사고다.** 콘솔은 개인키를 붙여넣으면 거절한다.

**2) 웹 콘솔 → [접속 키]** 에 그 한 줄을 붙여넣고 저장한다.

등록한 키는 **배정받은 랩의 노드에만** 들어간다. 남의 랩에는 들어가지 않는다.
저장한 뒤 **[지금 랩에 반영]** 을 누른다.

**3) 같은 화면에서 [내 SSH 설정 내려받기]** 를 누른다.
`ssh-config-lab{{ lab_id }}` 파일이 **다운로드 폴더**에 받아진다. 그 내용을 설정에 붙인다.

```powershell
New-Item -ItemType Directory -Force $HOME\.ssh | Out-Null
Get-Content $HOME\Downloads\ssh-config-lab{{ lab_id }} | Add-Content $HOME\.ssh\config
```

<details><summary>macOS · Linux 를 쓴다면</summary>

```bash
ssh-keygen -t ed25519 -C "내이름@내PC"      # 세 번 Enter
cat ~/.ssh/id_ed25519.pub                   # 이 한 줄을 콘솔에 붙여넣는다

mkdir -p ~/.ssh
cat ~/Downloads/ssh-config-lab{{ lab_id }} >> ~/.ssh/config
chmod 600 ~/.ssh/config
```

</details>

> 랩 노드는 사무실에서 **직접 보이지 않는다.** 이 설정이 하는 일은
> "운영 서버를 거쳐 들어가라"를 `ssh` 에게 알려주는 것이다(ProxyJump).
> 왜 직접 안 보이게 했는지는 M9 에서 다룬다 — 지금은 "관리망은 따로 있다"만 알면 된다.

### 실습 2 — SSH 로 들어가기

```bash
ssh pc1
```

들어가면 이런 화면이 보인다.

```
  ┌──────────────────────────────────────────────────────────────┐
   pc1      host        lab{{ lab_id }}   단계: {{ stage }}
   {{ nodes.pc1.desc }}
  └──────────────────────────────────────────────────────────────┘
   lab-whereami   내 인터페이스가 어느 링크에 붙어 있는지 확인
   {{ mgmt_if }} 는 관리망이다. 통신 테스트는 eth1 이후로 할 것.
```

**이제부터 실습은 전부 이 터미널에서 한다.** 웹 콘솔은 교재를 읽고 랩을 준비하는 곳이다.

<details><summary>안 되면</summary>

`ssh pc1` 은 **두 번 로그인한다.** 먼저 운영 서버(점프 호스트), 그다음 랩 노드.
그래서 어디서 막혔는지부터 봐야 한다.

```bash
ssh -v pc1
```

`Authenticating to <주소>` 줄의 주소가 `{{ jump_ip }}` 면 **점프 호스트**,
`{{ mgmt.pc1 }}` 면 **랩 노드**에서 막힌 것이다.

| 증상 | 뜻 | 할 일 |
|---|---|---|
| **비밀번호를 묻는다** (점프 호스트에서) | 점프 계정이 아직 없다 | 교육 담당자에게 **점프 계정 발급** 요청 |
| **비밀번호를 묻는다** (랩 노드에서) | 내 키가 아직 노드에 없다 | [접속 키] → **[지금 랩에 반영]** |
| `Permission denied (publickey)` | 위 둘 중 하나 | `ssh -v` 로 어느 쪽인지 확인 |
| `Could not resolve hostname pc1` | `~/.ssh/config` 가 안 붙었다 | 실습 1 의 3) 을 다시 |
| `ssh : 용어가 인식되지 않습니다` | Windows 에 SSH 가 꺼져 있다 | 설정 → 선택적 기능 → `OpenSSH 클라이언트` 추가 |
| `Bad owner or permissions` | 설정 파일 권한 (macOS·Linux) | `chmod 600 ~/.ssh/config` |

> **비밀번호를 물으면 그건 이미 실패한 것이다.** 이 랩은 어디서도 비밀번호로 로그인하지
> 않는다 — 넣을 비밀번호가 존재하지 않는다. 받은 설정에는 비밀번호 시도를 꺼 두어
> `Permission denied (publickey)` 로 바로 나오게 해 두었다.
> 예전 설정을 쓰고 있다면 **[내 SSH 설정 내려받기]** 로 다시 받을 것.

노드에 들어간 뒤 `cat /etc/lab-authorized` 를 보면 **누가 이 노드에 들어올 수 있는지** 나온다.

</details>

### 실습 3 — 내가 어디에 있는지 확인하기

Proxmox 는 토폴로지를 그림으로 보여주지 않는다. 대신 이 명령을 쓴다.

```bash
lab-whereami
```

```
pc1 (host) — {{ nodes.pc1.desc }}

인터페이스 상태     주소                 상대노드       설명
-------------------------------------------------------------------------------
{{ "%-10s"|format(mgmt_if) }} up       {{ "%-20s"|format(mgmt.pc1 + "/" + nodes.pc1.mgmt_prefixlen|string) }} (관리망)      OOB — 통신 테스트에 쓰지 말 것
eth1       up       {{ "%-20s"|format(ipc.pc1) }} sw1            access [VLAN 10]

단계: {{ stage }}   |   전체 지도: dist/lab-map.md
```

읽는 법:
- `{{ mgmt_if }}` = 관리망. 지금 당신이 들어온 길.
- `eth1` = 서비스망. **sw1 에 연결**되어 있다. 실습 대상.

### 실습 4 — 서비스망을 죽여도 SSH 는 살아 있다

```bash
# pc1 에서
sudo ip link set eth1 down
lab-whereami
```

`eth1` 이 `down` 으로 바뀐다. 그런데 **SSH 는 끊기지 않는다.**

```bash
exit
ssh pc1          # 다시 들어가진다
```

> **왜?** SSH 는 `{{ mgmt_if }}`(관리망)로 들어왔기 때문이다. `eth1` 과는 아무 상관이 없다.

되돌린다.

```bash
sudo ip link set eth1 up
```

### 실습 5 — 관리망을 죽이면 어떻게 되나 ★

이번엔 반대로 해본다. **접속이 끊길 것이다. 그게 목적이다.**

```bash
# pc1 에서
sudo ip link set {{ mgmt_if }} down
```

터미널이 멈춘다. `Ctrl+C` 도 안 먹는다. 잠시 후 연결이 끊긴다.

```bash
ssh pc1
# ssh: connect to host {{ mgmt.pc1 }} port 22: No route to host
```

**이제 SSH 로는 절대 못 들어간다.** 스스로 접속 경로를 끊었기 때문이다.

#### 복구 — 콘솔로 들어간다

1. 브라우저 → `{{ console_url }}` → 발급받은 **Proxmox 계정**으로 로그인
   (Realm 을 *Proxmox VE authentication server* 로 고를 것 — Linux PAM 이 아니다)
2. 왼쪽 목록에서 `{{ nodes.pc1.vm_name }}` → **Console**
3. `{{ lab_user }}` / 웹 콘솔 **[접속 키] → 5. 콘솔** 에 있는 비밀번호로 로그인
4. 링크를 다시 올린다:

```bash
sudo ip link set {{ mgmt_if }} up
```

5. 자기 PC 에서 다시 `ssh pc1` → 접속된다.

> 목록에 **자기 랩 VM 만** 보인다. 남의 랩 화면은 열리지 않는다.
> 그리고 이 계정으로는 VM 을 끄거나 지울 수 없다 — 화면을 보는 권한뿐이다.

> **이것이 콘솔이 존재하는 이유다.**
> 실무에서 원격지 장비의 인터페이스를 잘못 내리면 똑같은 일이 벌어진다.
> 그때 콘솔 서버가 없으면 **차를 타고 현장에 가야 한다.**
>
> SSH 만 알고 있는 엔지니어는 자기가 만든 장애를 스스로 복구하지 못한다.

### 실습 6 — 초기화

무엇을 망가뜨렸든 되돌릴 수 있다. 관리자에게 요청하거나, 권한이 있다면 직접:

```bash
# 점프 호스트에서
make reset LAB={{ lab_id }} STAGE={{ stage }}
```

1분 안에 랩이 처음 상태로 돌아온다.

> **그러니 마음껏 망가뜨려라.**
> 이 랩은 실험하라고 있는 것이다. 되돌리는 비용이 싸야 사람은 과감해진다.

---

## 5. 앞으로 쓸 도구

지금 다 이해할 필요는 없다. **어떤 질문에 어떤 도구를 쓰는지**만 봐 두면 된다.

| 질문 | 도구 | 배우는 모듈 |
|---|---|---|
| 내 인터페이스·주소·경로는? | `ip addr` / `ip link` / `ip route` | M1~M3 |
| 상대까지 닿나? | `ping` | M1 |
| 어느 경로로 가나? 어디서 막히나? | `traceroute` | M3 |
| 상대 MAC 은? | `ip neigh` (ARP) | M1 |
| 스위치가 뭘 알고 있나? | `bridge fdb show` | M1 |
| 포트가 열려 있나? | `telnet` / `nc` | M6 |
| 내 세션 상태는? | `ss -tanp` | M6 |
| **실제로 무슨 패킷이 오갔나?** | `tcpdump` | **M1부터 계속** |
| 웹 서버 응답은? | `curl -v` | M8 |
| 이름 해석은 되나? | `dig` | M8 |

> `tcpdump` 는 마지막에 배우는 고급 도구가 아니다.
> **M1부터 매 모듈에서 쓴다.** "그런 것 같다"가 아니라 "패킷으로 확인했다"고 말하는 습관을 들이기 위해서다.

---

## 6. 흔한 오해

| 오해 | 사실 |
|---|---|
| "ping 되면 통신 정상이다" | ping 은 **L3 도달성**만 본다. 포트가 닫혀 있어도 ping 은 된다. (M6) |
| "관리망 IP 로 확인하면 된다" | 관리망은 랩과 분리돼 있어 **항상** 통한다. 검증이 되지 않는다. |
| "SSH 만 있으면 된다" | 자기가 인터페이스를 내리는 순간 아무것도 못 한다. 콘솔이 최후의 보루다. |
| "랩을 망가뜨리면 안 된다" | 반대다. 망가뜨려야 배운다. 초기화는 1분이면 된다. |

---

## 다음

**[M1 — 랜선 한 가닥부터: L1/L2와 스위칭](../m01-l2-switching/README.md)**

과제와 확인 문제는 [tasks.md](tasks.md) 에 있다. 다음 모듈로 넘어가기 전에 반드시 풀 것.
