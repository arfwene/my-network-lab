
  ┌──────────────────────────────────────────────────────────────┐
   {{ "%-8s"|format(node) }} {{ "%-10s"|format(node_role) }}  lab{{ lab_id }}   단계: {{ lab_stage }}
   {{ desc }}
  └──────────────────────────────────────────────────────────────┘
   lab-whereami   내 인터페이스가 어느 링크에 붙어 있는지 확인
   {{ mgmt_interface }} 는 관리망입니다. 통신 테스트는 eth1 이후로 해 주세요.

