#!/usr/bin/env python3
"""
평서형(`~한다`) 문장 끝을 존댓말(`~합니다`)로 바꾼다. 초벌만 뽑는다.

교재는 교육생이 읽는 글이므로 존댓말로 쓴다 (docs/STYLE.md 1). 모듈 하나에
문장 끝이 백 개 넘게 있어서 손으로 고치면 반드시 몇 개를 빠뜨린다.

▸ 왜 낱말을 표로 못 박았나
  한국어 활용을 규칙으로 짐작하면(`~다` -> `~습니다`) `드물다`가 `드물습니다`가
  되고 `크다`가 `크습니다`가 된다. 한 문서에 나오는 어미는 이백 개를 넘지 않으므로
  세어서 표로 두는 편이 확실하다. 표에 없는 낱말은 **건드리지 않고 남긴다** —
  `make style` 의 평서형 칸이 몇 개 남았는지 세어 주므로 놓치지 않는다.

▸ 건드리지 않는 것
  코드 블록 · 절 제목 · 지정한 시작 위치보다 앞(제목 · 도입 · 학습 목표).
  구성도(labdiagram) 안에서는 notes 줄만 바꾼다.

usage:  python3 tools/tone.py <파일> '<이 문자열부터>'
"""
import re, sys

M = {}
def add(*pairs):
    for a, b in pairs: M[a] = b

# 명사 + 이다/다
add(*[(a, (a[:-2] if a.endswith("이다") else a[:-1]) + "입니다") for a in
      "것이다 때문이다 상태다 하나다 값이다 단위다 둘이다 셋이다 뿐이다 절이다 "
      "설정뿐이다 줄뿐이다 그대로다 개다 둘뿐이다 법이다 핵심이다 뜻이다 단서다 "
      "이름표다 곳이다 번째다 프로토콜이다 덩어리다 결과다 장비다 사실이다 말이다 "
      "자체다 문제다 낭비다 여럿이다 따로다 증상이다 항목이다 부류다 경우다 이유다 "
      "시간이다 정상이다 블랙홀이다 확인이다 과제다 별개다 의도다 초다 여기까지다 "
      "차다 이다".split()])
# 형용사
add(("있다", "있습니다"), ("없다", "없습니다"), ("같다", "같습니다"), ("다르다", "다릅니다"),
    ("똑같다", "똑같습니다"), ("알맞다", "알맞습니다"), ("드물다", "드뭅니다"),
    ("흔하다", "흔합니다"), ("쉽다", "쉽습니다"), ("싸다", "쌉니다"),
    ("중요하다", "중요합니다"), ("편하다", "편합니다"), ("당연하다", "당연합니다"),
    ("잦다", "잦습니다"), ("짧다", "짧습니다"), ("크다", "큽니다"), ("멀쩡하다", "멀쩡합니다"),
    ("불안정하다", "불안정합니다"), ("나쁘다", "나쁩니다"), ("빠르다", "빠릅니다"),
    ("확실하다", "확실합니다"), ("맞다", "맞습니다"), ("이렇다", "이렇습니다"),
    ("아니다", "아닙니다"))
# 동사 (현재)
add(("한다", "합니다"), ("않는다", "않습니다"), ("된다", "됩니다"), ("본다", "봅니다"),
    ("보인다", "보입니다"), ("생긴다", "생깁니다"), ("쓴다", "씁니다"), ("넣는다", "넣습니다"),
    ("확인한다", "확인합니다"), ("선다", "섭니다"), ("둔다", "둡니다"), ("남는다", "남습니다"),
    ("계산한다", "계산합니다"), ("간다", "갑니다"), ("멈춘다", "멈춥니다"),
    ("박는다", "박습니다"), ("안다", "압니다"), ("적용된다", "적용됩니다"),
    ("깨진다", "깨집니다"), ("만든다", "만듭니다"), ("넘어간다", "넘어갑니다"),
    ("나간다", "나갑니다"), ("사라진다", "사라집니다"), ("죽는다", "죽습니다"),
    ("바꾼다", "바꿉니다"), ("버려진다", "버려집니다"), ("갖는다", "갖습니다"),
    ("바뀐다", "바뀝니다"), ("고른다", "고릅니다"), ("달라진다", "달라집니다"),
    ("되돌린다", "되돌립니다"), ("푼다", "풉니다"), ("비교한다", "비교합니다"),
    ("맞춘다", "맞춥니다"), ("돌아온다", "돌아옵니다"), ("나온다", "나옵니다"),
    ("준다", "줍니다"), ("맞춰진다", "맞춰집니다"), ("나눈다", "나눕니다"),
    ("가른다", "가릅니다"), ("적는다", "적습니다"), ("걷어낸다", "걷어냅니다"),
    ("버린다", "버립니다"), ("가까워진다", "가까워집니다"), ("믿는다", "믿습니다"),
    ("뿌린다", "뿌립니다"), ("나뉜다", "나뉩니다"), ("보낸다", "보냅니다"),
    ("못한다", "못합니다"), ("반복한다", "반복합니다"), ("계산된다", "계산됩니다"),
    ("지정한다", "지정합니다"), ("조정한다", "조정합니다"), ("막는다", "막습니다"),
    ("들어간다", "들어갑니다"), ("전달한다", "전달합니다"), ("처리한다", "처리합니다"),
    ("통한다", "통합니다"), ("되돌아간다", "되돌아갑니다"), ("지원한다", "지원합니다"),
    ("판정한다", "판정합니다"), ("리셋된다", "리셋됩니다"), ("늘어난다", "늘어납니다"),
    ("뜻한다", "뜻합니다"), ("올라온다", "올라옵니다"), ("훑는다", "훑습니다"),
    ("번거로워진다", "번거로워집니다"), ("탄다", "탑니다"), ("살아남는다", "살아남습니다"),
    ("알아본다", "알아봅니다"), ("차지한다", "차지합니다"), ("광고한다", "광고합니다"),
    ("건다", "겁니다"), ("고친다", "고칩니다"), ("그린다", "그립니다"),
    ("반영된다", "반영됩니다"), ("하다", "합니다"), ("내보낸다", "내보냅니다"),
    ("채운다", "채웁니다"), ("돌린다", "돌립니다"), ("끊는다", "끊습니다"),
    ("센다", "셉니다"), ("감지한다", "감지합니다"), ("수렴한다", "수렴합니다"),
    ("옮긴다", "옮깁니다"), ("유지된다", "유지됩니다"), ("드러난다", "드러납니다"),
    ("줄어든다", "줄어듭니다"), ("세운다", "세웁니다"), ("누른다", "누릅니다"),
    ("돌아간다", "돌아갑니다"), ("말한다", "말합니다"), ("이긴다", "이깁니다"),
    ("결정한다", "결정합니다"), ("시작한다", "시작합니다"))
# 과거
add(*[(a, a[:-1] + "습니다") for a in
      "바뀌었다 넘겼다 넣었다 걸었다 됐다 인식했다 같아졌다 늘었다 적었다 빠졌다 "
      "줬다 겹쳤다 않았다 만들어졌다 뿌렸다 했다 멈췄다 내려왔다 사라졌다 끝났다".split()])

WORD = re.compile("(" + "|".join(sorted(M, key=len, reverse=True)) + r")(?=[.\s]|$)")
FENCE = re.compile(r"^```")


def convert(line):
    return WORD.sub(lambda m: M[m.group(1)], line)


def main(path, start):
    src = open(path, encoding="utf-8").read()
    head, body = src[:src.index(start)], src[src.index(start):]
    out, fence, diag = [], False, False
    for line in body.split("\n"):
        if FENCE.match(line):
            diag = not fence and line.startswith("```labdiagram")
            fence = not fence
            out.append(line)
            continue
        if line.startswith("#") or line.startswith("|---"):
            out.append(line)
            continue
        if fence and not (diag and re.match(r"^  - [^{]", line)):
            out.append(line)
            continue
        out.append(convert(line))
    open(path, "w", encoding="utf-8").write(head + "\n".join(out))


if __name__ == "__main__":
    if len(sys.argv) != 3:
        sys.exit(__doc__.strip().splitlines()[-1])
    main(sys.argv[1], sys.argv[2])
